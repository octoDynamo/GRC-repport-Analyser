% =============================================================================
%  Rapport de Projet de Fin d'Etudes - GRC AI Analyzer
%  Page de garde : modele ENSEM 2019-2020
%  Compilation : pdflatex rapport_pfe.tex (x3 pour TOC + references)
% =============================================================================

\documentclass[a4paper,12pt,oneside]{report}

% --------------------- Encodage et langue ---------------------
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage[french]{babel}
\usepackage{csquotes}

% --------------------- Mise en page ---------------------
\usepackage[a4paper,top=2.5cm,bottom=2.5cm,left=2.5cm,right=2.5cm]{geometry}
\usepackage{setspace}
\onehalfspacing
\setlength{\headheight}{14.5pt}
\setlength{\parindent}{1.5em}
\setlength{\parskip}{6pt}

% --------------------- Maths et symboles ---------------------
\usepackage{amsmath}
\usepackage{amssymb}

% --------------------- Palette graphique (theme moderne) ---------------------
\usepackage[table,dvipsnames]{xcolor}
% -- Couleurs primaires
\definecolor{darknavy}{HTML}{1B2A4A}    % bleu nuit profond (titres, cadres)
\definecolor{titleblue}{HTML}{1F3A66}   % bleu titres
\definecolor{accentblue}{HTML}{2E6FB7}  % bleu accent (filets, liens)
\definecolor{accentteal}{HTML}{0E7C86}  % sarcelle (accents secondaires)
% -- Couleurs neutres / fonds
\definecolor{lightgray}{HTML}{EAEEF4}   % fond clair (lignes alternees)
\definecolor{rowalt}{HTML}{F5F7FB}      % zebrure de tableau
\definecolor{boxgray}{HTML}{F6F8FB}     % fond des encadres
\definecolor{headerblue}{HTML}{1F3A66}  % fond d'en-tete de tableau
\definecolor{rulegray}{HTML}{C7D0DD}    % filets discrets
% -- Couleurs semantiques
\definecolor{darkgreen}{HTML}{1B6B3A}   % succes / validation
\definecolor{warnamber}{HTML}{B8860B}   % avertissement
\definecolor{dangerred}{HTML}{B23B3B}   % alerte / non-conformite

% --------------------- Tableaux ---------------------
\usepackage{array}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{tabularx}
\usepackage{colortbl}
\usepackage{makecell}
\renewcommand{\arraystretch}{1.35}
\setlength{\arrayrulewidth}{0.4pt}
\arrayrulecolor{rulegray}
% Macros de tableau moderne (en-tete colore + texte blanc)
\newcommand{\colhead}[1]{\textcolor{white}{\bfseries #1}}
\newcommand{\headrow}{\rowcolor{headerblue}}
% colonne centree de largeur fixe
\newcolumntype{C}[1]{>{\centering\arraybackslash}p{#1}}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
% symboles de comparaison colores
\newcommand{\cyes}{\textcolor{darkgreen}{\ding{51}}}
\newcommand{\cno}{\textcolor{dangerred}{\ding{55}}}

% --------------------- Flottants ---------------------
\usepackage{float}

% --------------------- Graphiques ---------------------
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric,arrows.meta,positioning,fit,calc,backgrounds}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{shapes.arrows, shapes.misc}

% --------------------- Listes ---------------------
\usepackage{enumitem}
\usepackage{pifont}

% --------------------- Boites et encadres ---------------------
\usepackage[most]{tcolorbox}
\tcbset{
	% Encadre "problematique" : barre laterale accent, fond clair
	problemboxstyle/.style={
		enhanced, breakable,
		colback=boxgray, colframe=accentblue,
		boxrule=0pt, leftrule=3pt, arc=2pt,
		left=12pt, right=12pt, top=10pt, bottom=10pt,
		fontupper=\itshape,
		borderline west={3pt}{0pt}{accentblue}
	},
	% Encadre de synthese / critique
	critiquestyle/.style={
		enhanced, breakable,
		colback=boxgray, colframe=darknavy,
		boxrule=0.6pt, arc=3pt,
		left=12pt, right=12pt, top=10pt, bottom=10pt,
		drop fuzzy shadow=rulegray
	},
	% Encadre informatif (chapeau de chapitre)
	infobox/.style={
		enhanced, breakable,
		colback=lightgray, colframe=lightgray,
		boxrule=0pt, arc=3pt,
		left=12pt, right=12pt, top=8pt, bottom=8pt,
		borderline west={3pt}{0pt}{accentteal}
	}
}
% Boite a titre reutilisable (en-tete colore)
\newtcolorbox{titledbox}[2][]{%
	enhanced, breakable,
	colback=boxgray, colframe=darknavy,
	coltitle=white, fonttitle=\bfseries,
	colbacktitle=darknavy,
	boxrule=0.6pt, arc=3pt,
	left=11pt, right=11pt, top=8pt, bottom=8pt,
	title={#2}, #1
}

% --------------------- Code source ---------------------
\usepackage{listings}
\lstdefinestyle{pythonstyle}{
	language=Python,
	basicstyle=\ttfamily\footnotesize,
	keywordstyle=\color{accentblue}\bfseries,
	stringstyle=\color{darkgreen},
	commentstyle=\color{gray}\itshape,
	numbers=left,
	numberstyle=\tiny\color{rulegray},
	numbersep=10pt,
	backgroundcolor=\color{boxgray},
	frame=lines,
	framesep=6pt,
	rulecolor=\color{rulegray},
	breaklines=true,
	showstringspaces=false,
	tabsize=2,
	xleftmargin=14pt,
	aboveskip=10pt, belowskip=10pt,
	captionpos=b
}
\lstset{style=pythonstyle}

% --------------------- Titres et chapitres ---------------------
\usepackage{titlesec}

% Chapitre : grand numero accent a gauche + filet sous le titre
\titleformat{\chapter}[hang]
{\normalfont\huge\bfseries\color{titleblue}}
{\hspace*{-2pt}\colorbox{darknavy}{\parbox[c][1.15cm]{1.15cm}{\centering\color{white}\fontsize{34}{34}\selectfont\thechapter}}\hspace{14pt}}
{0pt}
{\Huge}
[\vspace{4pt}{\color{accentblue}\titlerule[1.2pt]}]
\titlespacing*{\chapter}{0pt}{-18pt}{26pt}

% Chapitres etoiles (preliminaires) : titre + filet, sans numero
\titleformat{name=\chapter,numberless}[hang]
{\normalfont\Huge\bfseries\color{titleblue}}
{}{0pt}{}
[\vspace{4pt}{\color{accentblue}\titlerule[1.2pt]}]

\titleformat{\section}
{\normalfont\Large\bfseries\color{titleblue}}
{\textcolor{accentblue}{\thesection}}{0.7em}{}
\titleformat{\subsection}
{\normalfont\large\bfseries\color{titleblue!88}}
{\textcolor{accentblue!85}{\thesubsection}}{0.7em}{}
\titleformat{\subsubsection}
{\normalfont\normalsize\bfseries\color{accentteal}}
{\thesubsubsection}{0.7em}{}

% --------------------- En-tete / pied de page ---------------------
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\footnotesize\color{titleblue}\textbf{GRC AI Analyzer}}
\fancyhead[R]{\footnotesize\color{darknavy!70}\textit{Rapport de Projet de Fin d'\'Etudes}}
\fancyfoot[C]{\footnotesize\color{darknavy!75}\thepage}
\renewcommand{\headrulewidth}{0.8pt}
\renewcommand{\footrulewidth}{0pt}
\renewcommand{\headrule}{\hbox to\headwidth{\color{accentblue}\leaders\hrule height \headrulewidth\hfill}}

\fancypagestyle{plain}{
	\fancyhf{}
	\fancyfoot[C]{\footnotesize\color{darknavy!75}\thepage}
	\renewcommand{\headrulewidth}{0pt}
	\renewcommand{\footrulewidth}{0pt}
}

% --------------------- Liens ---------------------
\usepackage[colorlinks=true,linkcolor=accentblue,urlcolor=accentblue,citecolor=accentblue]{hyperref}
\usepackage{url}

% =============================================================================
\begin{document}
	
	% =============================================================================
	%  PAGES PRELIMINAIRES
	% =============================================================================
	\pagenumbering{Roman}
	
	% --- Dedicace ---
	\chapter*{D\'edicace}
	\addcontentsline{toc}{chapter}{D\'edicace}
	\thispagestyle{plain}
	
	\begin{quote}\itshape
		C'est avec une profonde gratitude et une immense fiert\'e que je d\'edie ce modeste travail, fruit de plusieurs mois d'efforts, de pers\'ev\'erance et de recherche, \`a toutes les personnes qui ont contribu\'e \`a mon parcours acad\'emique et personnel.
		
		\medskip
		En premier lieu, je d\'edie ce travail \`a moi-m\^eme, en hommage aux efforts fournis, aux d\'efis surmont\'es et \`a la d\'etermination qui m'a permis d'arriver \`a l'aboutissement de ce projet.
		
		\medskip
		Je d\'edie \'egalement ce rapport \`a mes chers parents, qui ont toujours \'et\'e ma source d'inspiration, de force et de motivation. Aucun mot ne saurait exprimer l'ampleur de ma reconnaissance pour leurs sacrifices, leur soutien inconditionnel, leur amour et leurs pri\`eres qui m'ont accompagn\'e tout au long de mon parcours.
		
		\medskip
		\`A mes deux fr\`eres, pour leur pr\'esence, leur soutien et les moments de partage qui ont rendu ce chemin plus agr\'eable. Que ce travail soit le t\'emoignage de mon affection et de ma gratitude envers eux.
		
		\medskip
		Je d\'edie ce projet \`a mes encadrants, pour leur accompagnement, leurs conseils pr\'ecieux et leur disponibilit\'e, qui ont \'et\'e d'une grande importance dans la r\'ealisation de ce travail de fin d'\'etudes.
		
		\medskip
		Enfin, je d\'edie ce travail \`a mes professeurs, mes amis et toutes les personnes qui me sont ch\`eres, pour leur encouragement, leur soutien et leur contribution, de pr\`es ou de loin, \`a mon \'evolution et \`a l'accomplissement de ce projet.
	\end{quote}
	
	% --- Remerciements ---
	\chapter*{Remerciements}
	\addcontentsline{toc}{chapter}{Remerciements}
	\thispagestyle{plain}
	
	Tout d'abord, je tiens \`a remercier Allah, Le Tout-Puissant, pour Sa gr\^ace, Sa b\'en\'ediction et la force qu'Il m'a accord\'ee tout au long de mon parcours, me permettant ainsi d'atteindre l'aboutissement de ce travail. 
	
	Je tiens ensuite \`a exprimer ma profonde gratitude \`a toutes les personnes qui ont contribu\'e, de pr\`es ou de loin, \`a la r\'ealisation de ce travail, ainsi qu'\`a l'accomplissement de mon parcours acad\'emique et personnel.
	
	Tout d'abord, j'adresse mes plus sinc\`eres remerciements \`a mes chers parents, pour leur amour inconditionnel, leurs sacrifices, leur patience et leur soutien constant. Leur confiance, leurs encouragements et leurs pri\`eres ont \'et\'e une source de motivation essentielle tout au long de mon parcours.
	
	Je remercie \'egalement mes deux fr\`eres pour leur pr\'esence, leur soutien moral et les moments de partage qui ont accompagn\'e les diff\'erentes \'etapes de ma vie.
	
	Je tiens \`a exprimer ma reconnaissance \`a mon encadrant p\'edagogique, M.~Mehdi FARAH, pour son accompagnement, sa disponibilit\'e, ses conseils pertinents et son suivi rigoureux, qui ont grandement contribu\'e \`a la r\'eussite de ce projet.
	
	Mes sinc\`eres remerciements vont aussi \`a toute l'\'equipe de travail au sein de l'entreprise, pour son accueil, sa confiance, son esprit collaboratif et le partage de ses connaissances et de son exp\'erience professionnelle, qui ont enrichi cette p\'eriode de stage.
	
	Je remercie \'egalement l'ensemble de mes professeurs pour la qualit\'e de la formation dispens\'ee, leurs conseils et les connaissances transmises tout au long de mon parcours universitaire.
	
	J'adresse aussi mes remerciements \`a mes amis pour leur soutien, leurs encouragements et les moments partag\'es qui ont rendu ce parcours plus agr\'eable.
	
	Enfin, je tiens \`a adresser une pens\'ee particuli\`ere \`a une personne tr\`es ch\`ere \`a mon c\oe ur, pour son soutien inestimable, sa pr\'esence, ses encouragements et son accompagnement durant toutes les \'etapes de ma vie. Son Amoure, sa confiance et son soutien ont \'et\'e une force pr\'ecieuse dans la r\'ealisation de ce travail.
	
	% --- Resume ---
	\chapter*{R\'esum\'e}
	\addcontentsline{toc}{chapter}{R\'esum\'e}
	\thispagestyle{plain}
	
	Dans un contexte mondial marqu\'e par la multiplication des r\'eglementations en mati\`ere de protection des donn\'ees et de cybers\'ecurit\'e, les organisations sont confront\'ees \`a une charge croissante d'analyse de leurs rapports de gouvernance, de risque et de conformit\'e (GRC). L'analyse manuelle de ces documents est chronophage, sujette aux erreurs et difficilement scalable.
	
	C'est dans ce cadre que s'inscrit ce projet de fin d'\'etudes, dont l'objectif est la conception et la r\'ealisation de \textbf{GRC AI Analyzer}, une plateforme \emph{full-stack} d'analyse automatis\'ee des rapports GRC propuls\'ee par l'intelligence artificielle g\'en\'erative. Le syst\`eme permet l'import de documents aux formats PDF, DOCX et XLSX, puis exploite le mod\`ele \textbf{Mistral AI} pour extraire automatiquement les risques, \'evaluer la conformit\'e vis-\`a-vis des r\'ef\'erentiels \textbf{ISO~27001}, \textbf{RGPD} et de la \textbf{Loi 09-08} marocaine, et g\'en\'erer des recommandations concr\`etes.
	
	L'architecture repose sur un backend \textbf{FastAPI} asynchrone, une persistance \textbf{PostgreSQL}, un stockage objet \textbf{MinIO}, une base vectorielle \textbf{ChromaDB} pour le \emph{Retrieval Augmented Generation} (RAG), et une interface utilisateur \textbf{React/Vite} moderne. Un pipeline d'analyse en deux phases parall\`eles (phase~1~: 5~t\^aches simultan\'ees dont 4~appels LLM~; phase~2~: 2~appels LLM) assure des temps de traitement r\'eduits, tandis qu'une interface conversationnelle bas\'ee sur le RAG permet \`a l'utilisateur d'interroger directement le rapport analys\'e.
	
	\medskip
	\textbf{Mots-cl\'es :} GRC, Intelligence Artificielle, Mistral, RAG, ChromaDB, FastAPI, React, ISO~27001, RGPD, Loi 09-08, conformit\'e, gestion des risques.
	
	% --- Abstract ---
	\chapter*{Abstract}
	\addcontentsline{toc}{chapter}{Abstract}
	\thispagestyle{plain}
	
	In a global context marked by the multiplication of regulations regarding data protection and cybersecurity, organizations face a growing burden in analyzing their Governance, Risk and Compliance (GRC) reports. Manual analysis of these documents is time-consuming, error-prone and hardly scalable.
	
	This end-of-studies project introduces \textbf{GRC AI Analyzer}, a full-stack platform for automated GRC report analysis powered by generative artificial intelligence. The system enables document upload in PDF, DOCX and XLSX formats, then leverages the \textbf{Mistral AI} model to automatically extract risks, evaluate compliance against \textbf{ISO~27001}, \textbf{GDPR} and Morocco's \textbf{Law 09-08}, and generate concrete recommendations.
	
	The architecture relies on an asynchronous \textbf{FastAPI} backend, a \textbf{PostgreSQL} database, a \textbf{MinIO} object store, a \textbf{ChromaDB} vector database for Retrieval Augmented Generation (RAG), and a modern \textbf{React/Vite} user interface. A two-phase asyncio.gather pipeline (5 tasks in phase 1 including 4 LLM calls, 2 LLM calls in phase 2) ensures reduced processing times, while a conversational interface based on RAG allows users to directly interrogate analyzed reports.
	
	\medskip
	\textbf{Keywords:} GRC, Artificial Intelligence, Mistral, RAG, ChromaDB, FastAPI, React, ISO~27001, GDPR, Law 09-08, compliance, risk management.
	
	% --- TOC, LOF, LOT ---
	\renewcommand{\contentsname}{Table des mati\`eres}
	\tableofcontents
	\listoffigures
	\listoftables
	
	\chapter*{Liste des acronymes}
	\addcontentsline{toc}{chapter}{Liste des acronymes}
	
	\begin{description}[leftmargin=2.5cm,style=nextline,itemsep=2pt]
		\item[\textbf{AIPD}] Analyse d'Impact relative \`a la Protection des Donn\'ees
		\item[\textbf{API}] Application Programming Interface
		\item[\textbf{ASGI}] Asynchronous Server Gateway Interface
		\item[\textbf{CISO}] Chief Information Security Officer
		\item[\textbf{CNDP}] Commission Nationale de contr\^ole de la protection des Donn\'ees \`a caract\`ere Personnel (Maroc)
		\item[\textbf{CORS}] Cross-Origin Resource Sharing
		\item[\textbf{CRUD}] Create, Read, Update, Delete
		\item[\textbf{CSP}] Content Security Policy
		\item[\textbf{CVE}] Common Vulnerabilities and Exposures
		\item[\textbf{CVSS}] Common Vulnerability Scoring System
		\item[\textbf{CWE}] Common Weakness Enumeration
		\item[\textbf{DPO}] Data Protection Officer (D\'el\'egu\'e \`a la Protection des Donn\'ees)
		\item[\textbf{DTO}] Data Transfer Object
		\item[\textbf{GRC}] Governance, Risk and Compliance
		\item[\textbf{HMR}] Hot Module Replacement
		\item[\textbf{HNSW}] Hierarchical Navigable Small World (algorithme d'indexation vectorielle)
		\item[\textbf{ISO}] International Organization for Standardization
		\item[\textbf{JWT}] JSON Web Token
		\item[\textbf{LLM}] Large Language Model
		\item[\textbf{MinIO}] Logiciel de stockage objet open-source compatible S3
		\item[\textbf{NIST}] National Institute of Standards and Technology
		\item[\textbf{ORM}] Object Relational Mapping
		\item[\textbf{OWASP}] Open Web Application Security Project
		\item[\textbf{PDF}] Portable Document Format
		\item[\textbf{PFE}] Projet de Fin d'\'Etudes
		\item[\textbf{RAG}] Retrieval Augmented Generation
		\item[\textbf{REST}] Representational State Transfer
		\item[\textbf{RGPD}] R\`eglement G\'en\'eral sur la Protection des Donn\'ees (EU 2016/679)
		\item[\textbf{RSSI}] Responsable de la S\'ecurit\'e des Syst\`emes d'Information
		\item[\textbf{SMSI}] Syst\`eme de Management de la S\'ecurit\'e de l'Information
		\item[\textbf{SPA}] Single Page Application
		\item[\textbf{SQL}] Structured Query Language
		\item[\textbf{UI}] User Interface
		\item[\textbf{UX}] User Experience
	\end{description}
	
	% =============================================================================
	\clearpage
	\pagenumbering{arabic}
	% =============================================================================
	
	% =============================================================================
	%  CHAPITRE 1 — CONTEXTE DU PROJET
	% =============================================================================
	\chapter{Contexte du projet}
	
	\section{Introduction}
	Ce premier chapitre a pour objectif de placer le projet dans son environnement professionnel, m\'etier et r\'eglementaire. Avant d'aborder l'analyse d\'etaill\'ee des besoins et la conception de la solution, il est n\'ecessaire de comprendre le contexte dans lequel s'inscrit la cr\'eation de \emph{GRC AI Analyzer}, une plateforme d'analyse automatis\'ee des rapports de gouvernance, risque et conformit\'e propuls\'ee par l'intelligence artificielle.
	
	Le chapitre pr\'esente d'abord l'organisme d'accueil, ainsi que le contexte m\'etier de la GRC. Il expose ensuite les enjeux r\'eglementaires (ISO~27001, RGPD, Loi 09-08), formule la probl\'ematique du projet et pr\'esente la solution et les objectifs envisag\'es.
	
	% =============================================================================
	%  SECTION 1 — PRÉSENTATION DE L'ORGANISME D'ACCUEIL
	% =============================================================================
	\section{Pr\'esentation de l'organisme d'accueil}
	
	\subsection{Pr\'esentation g\'en\'erale}
	Evolvity Partners est un cabinet marocain de conseil sp\'ecialis\'e en cybers\'ecurit\'e et en gestion des risques, fond\'e en 2022. S'appuyant sur un pool d'experts capitalisant plus de douze ann\'ees d'exp\'erience cumul\'ee, la structure a connu une croissance rapide~: deux consultants \`a sa cr\'eation, quatre en 2024, dix en 2025, avec une projection d'expansion internationale \`a l'horizon 2027. Ses locaux accueillent aujourd'hui une \'equipe pluridisciplinaire d\'edi\'ee \`a accompagner ses clients au Maroc et \`a l'international, selon une approche sur mesure conforme aux standards internationaux les plus exigeants. 
	\subsection{Domaines d'expertise et offre de services}
	
	L'offre d'Evolvity Partners est structur\'ee autour de quatre p\^oles compl\'ementaires couvrant l'ensemble du spectre de la cybers\'ecurit\'e.
	
	\begin{itemize}
		\item \textbf{Audit~:} Le p\^ole Audit d\'eploie des missions d'\'evaluation \`a plusieurs niveaux~:Organisationnel, Technique et Physique.
		\item \textbf{GRC~:} Le p\^ole GRC constitue le c\oe ur strat\'egique de l'activit\'e et couvre~: la gestion des risques, les audits de conformit\'e (ISO~27001, RGPD, Loi~09-08), la r\'edaction de politiques de s\'ecurit\'e et l'accompagnement \`a la certification.
		\item \textbf{Technologie~:} Le p\^ole Technologie couvre les domaines \'emergents~: Cloud, Blockchain, IoT et OT, avec une capacit\'e d'int\'egration de solutions s\'ecuris\'ees dans des environnements complexes et h\'et\'erog\`enes.
		\item \textbf{ISS As A Service~:} Ce mod\`ele de prestation externalis\'ee met \`a disposition des clients des profils sp\'ecialis\'es en mode r\'egie ou forfait~: RSSI (\textit{Responsable de la S\'ecurit\'e des Syst\`emes d'Information}), SecOps, PMO et experts m\'etier, offrant ainsi une flexibilit\'e adapt\'ee aux organisations ne disposant pas de ces comp\'etences en interne.
		
	\end{itemize}
	
	\subsection{Positionnement et client\`ele}
	
	Evolvity Partners s'adresse principalement aux banques, institutions financi\`eres, administrations publiques et grandes entreprises op\'erant dans des secteurs r\'eglement\'es. Son positionnement repose sur une approche qui allie rigueur technique, conformit\'e r\'eglementaire et gestion des risques strat\'egiques, avec pour objectif de prot\'eger les actifs num\'eriques de ses clients tout en accompagnant leur transformation digitale.
	
	\subsection{Cadre du projet GRC AI Analyzer}
	
	Le projet \textit{GRC AI Analyzer}, objet du pr\'esent rapport, s'inscrit directement dans le p\^ole GRC d'Evolvity Partners. Face au volume croissant de rapports d'audit, de politiques de s\'ecurit\'e et de documents de conformit\'e trait\'es par les consultants, l'entreprise a identifi\'e un besoin strat\'egique d'automatisation de l'analyse documentaire. Le projet vise \`a industrialiser cette pratique gr\^ace \`a une plateforme propuls\'ee par l'intelligence artificielle g\'en\'erative, r\'eduisant ainsi les d\'elais d'analyse et augmentant la pr\'ecision des diagnostics de conformit\'e.
	
	\section{Contexte m\'etier : la Gouvernance, le Risque et la Conformit\'e (GRC)}
	
	La GRC (\textit{Governance, Risk and Compliance}) d\'esigne le cadre int\'egr\'e par lequel une organisation structure et coordonne ses pratiques de gouvernance, de gestion des risques et de conformit\'e r\'eglementaire. Ces trois dimensions forment un syst\`eme coh\'erent~: une gouvernance solide oriente la gestion des risques, qui elle-m\^eme conditionne la capacit\'e de l'organisation \`a satisfaire ses obligations de conformit\'e. Dans un contexte o\`u les cybermenaces se multiplient et o\`u les exigences r\'eglementaires s'intensifient, la GRC est devenue un levier strat\'egique indispensable pour toute organisation souhaitant ma\^itriser son exposition et pr\'eserver la confiance de ses parties prenantes.
	
	\subsection{Les trois piliers de la GRC}
	
	\subsubsection*{Gouvernance}
	La gouvernance d\'efinit les orientations strat\'egiques et les responsabilit\'es en mati\`ere de s\'ecurit\'e de l'information. Elle comprend :
	\begin{itemize}[leftmargin=1.5em,itemsep=2pt]
		\item les politiques et chartes de s\'ecurit\'e ;
		\item les r\^oles cl\'es (RSSI, DPO, comit\'es) ;
		\item le suivi via des indicateurs de performance ;
		\item la sensibilisation et la formation des collaborateurs.
	\end{itemize}
	Elle constitue la base de la gestion des risques et de la conformit\'e.
	
	\subsubsection*{Gestion des risques}
	La gestion des risques consiste \`a identifier, analyser et traiter les menaces pouvant affecter l'organisation. Les principales \'etapes sont :
	\begin{enumerate}[leftmargin=1.5em,itemsep=2pt]
		\item identification des actifs et menaces ;
		\item analyse de l'impact et de la probabilit\'e ;
		\item prioritisation des risques ;
		\item traitement : r\'eduction, acceptation, transfert ou \'evitement ;
		\item suivi continu.
	\end{enumerate}
	Les r\'ef\'erences courantes incluent ISO~31000~\cite{iso31000}, ISO~27005~\cite{iso27005} et EBIOS Risk Manager.
	
	\subsubsection*{Conformit\'e}
	La conformit\'e garantit le respect des lois, normes et obligations applicables. Elle repose sur :
	\begin{itemize}[leftmargin=1.5em,itemsep=2pt]
		\item une veille r\'eglementaire ;
		\item des audits r\'eguliers ;
		\item des actions correctives ;
		\item la production de preuves et rapports.
	\end{itemize}
	Une bonne conformit\'e limite les risques juridiques, financiers et r\'eputationnels.
	\subsection{Cadres r\'eglementaires et normatifs retenus}
	
	Le projet GRC AI Analyzer s'appuie sur trois r\'ef\'erentiels compl\'ementaires couvrant la s\'ecurit\'e de l'information et la protection des donn\'ees au niveau international et marocain.
	
	\begin{table}[ht]
		\centering
		\begin{tabular}{@{}lll@{}}
			\toprule
			\headrow \colhead{R\'ef\'erentiel} & \colhead{Domaine} & \colhead{Port\'ee} \\
			\midrule
			ISO 27001:2022 & S\'ecurit\'e de l'information & Internationale \\
			\rowcolor{rowalt} RGPD & Protection des donn\'ees personnelles & Union Europ\'eenne \\
			Loi 09-08 & Protection des donn\'ees personnelles & Maroc \\
			\bottomrule
		\end{tabular}
		\caption{R\'ef\'erentiels couverts par la plateforme}
	\end{table}
	
	\subsubsection*{ISO 27001:2022}
	ISO~27001~\cite{iso27001} est la norme internationale de gestion de la s\'ecurit\'e de l'information. Elle d\'efinit les exigences d'un SMSI (\textit{Syst\`eme de Management de la S\'ecurit\'e de l'Information}). La version 2022 propose \textbf{93 contr\^oles}~\cite{iso27002} r\'eorganis\'es en 4~th\`emes (organisationnel, humain, physique, technologique). La version 2013 les structurait en \textbf{14~domaines} (A.5--A.18). Dans GRC AI Analyzer, l'\'evaluation de conformit\'e utilise les \textbf{14~domaines de l'Annexe~A (nomenclature 2013)} comme grille d'\'evaluation~--- approche justifi\'ee par la coh\'erence des r\'ef\'erentiels d'audit existants --- tout en r\'ef\'eren\c{c}ant ISO~27001:2022 comme norme cible.  
	La plateforme utilise ce r\'ef\'erentiel pour r\'ealiser des analyses d'\'ecarts (\textit{gap analysis}) et \'evaluer le niveau de conformit\'e des organisations.
	
	\subsubsection*{RGPD}
	Le RGPD~\cite{rgpd2016} est le r\`eglement europ\'een sur la protection des donn\'ees personnelles, applicable \`a toute organisation traitant des donn\'ees de citoyens europ\'eens.  
	Il repose sur plusieurs principes essentiels :
	\begin{itemize}[leftmargin=1.5em,itemsep=2pt]
		\item la lic\'eit\'e et la transparence des traitements ;
		\item la limitation des finalit\'es ;
		\item la minimisation des donn\'ees ;
		\item le respect des droits des personnes ;
		\item l'obligation de prouver la conformit\'e.
	\end{itemize}
	Le non-respect du RGPD peut entra\^\i ner des sanctions financi\`eres importantes.
	
	\subsubsection*{Loi 09-08}
	La Loi~09-08~\cite{loi0908} est le cadre marocain de protection des donn\'ees personnelles, supervis\'e par la CNDP.  
	Elle impose :
	\begin{itemize}[leftmargin=1.5em,itemsep=2pt]
		\item la d\'eclaration des traitements de donn\'ees ;
		\item le respect des droits des personnes concern\'ees ;
		\item la s\'ecurisation et la confidentialit\'e des donn\'ees ;
		\item le contr\^ole des transferts de donn\'ees \`a l'\'etranger.
	\end{itemize}
	Dans le contexte marocain, les entreprises doivent souvent respecter \`a la fois le RGPD et la Loi~09-08.
	
	\subsection{Enjeux de l'analyse documentaire GRC}
	
	Les cabinets de conseil en GRC produisent et analysent quotidiennement un volume consid\'erable de documents~: politiques de s\'ecurit\'e, rapports d'audit, plans de traitement des risques, preuves de conformit\'e. L'analyse manuelle de ces documents est chronophage, sujette aux erreurs d'interpr\'etation et difficile \`a standardiser entre consultants. C'est pr\'ecis\'ement ce verrou op\'erationnel que le projet GRC AI Analyzer vise \`a lever, en automatisant l'extraction, la structuration et l'\'evaluation des informations pertinentes contenues dans ces documents, au regard des exigences des r\'ef\'erentiels ISO~27001, RGPD et Loi~09-08.
	
	\section{Probl\'ematique du projet}
	
	L'analyse des rapports GRC pr\'esente une dualit\'e trompeuse, illustr\'ee par la m\'etaphore de l'iceberg~: ce qui est visible --- le rapport rendu et la conformit\'e d\'eclar\'ee, les recommandations transmises au client --- ne repr\'esente qu'une infime partie du travail r\'eel. La masse immerg\'ee, invisible au commanditaire, concentre l'essentiel de l'effort et des risques~: des dizaines d'heures d'analyse manuelle, d'annotations dispers\'ees et de relectures r\'ep\'et\'ees~; une interpr\'etation des normes qui varie selon l'expert, sans tra\c{c}abilit\'e des choix effectu\'es~; et surtout, des risques non d\'etect\'es et des priorit\'es fix\'ees \`a l'intuition, faute de temps ou de m\'ethode. C'est pr\'ecis\'ement cette partie invisible qui constitue le terreau des non-conformit\'es non identifi\'ees et des recommandations incompl\`etes.
	
	La probl\'ematique centrale peut d\`es lors \^etre formul\'ee comme suit~:
	
	\begin{tcolorbox}[problemboxstyle]
		Comment concevoir une plateforme intelligente capable d'automatiser l'analyse des rapports GRC h\'et\'erog\`enes (PDF, DOCX, XLSX), d'extraire les risques, d'\'evaluer la conformit\'e vis-\`a-vis des r\'ef\'erentiels ISO~27001, RGPD et Loi~09-08, et de fournir une interface conversationnelle exploitant le contenu analys\'e, tout en garantissant performance, fiabilit\'e et s\'ecurit\'e~?
	\end{tcolorbox}
	
	\section{Solution envisag\'ee}
	La solution propos\'ee, baptis\'ee \textbf{GRC AI Analyzer}, est une plateforme \emph{full-stack} compos\'ee d'un module d'ingestion documentaire prenant en charge PDF, DOCX et XLSX, d'un pipeline d'analyse asynchrone mobilisant le LLM Mistral AI, d'un moteur de Retrieval Augmented Generation pour le chat conversationnel, et d'une interface utilisateur React pour la visualisation et l'export des r\'esultats.
	
	\begin{figure}[ht]
		\centering
		\begin{tikzpicture}[node distance=1.4cm and 1.6cm]
			\tikzstyle{layer}=[rectangle,draw=darknavy,thick,minimum width=3.5cm,minimum height=1.1cm,rounded corners=3pt,align=center]
			\node[layer,fill=lightgray] (front) {\textbf{Frontend}\\React + Vite};
			\node[layer,fill=lightgray,below=of front] (back) {\textbf{Backend FastAPI}\\Async + JWT};
			\node[layer,fill=boxgray,below left=of back,xshift=-0.5cm] (db) {\textbf{PostgreSQL}\\M\'etadonn\'ees};
			\node[layer,fill=boxgray,below=of back] (minio) {\textbf{MinIO}\\Stockage fichiers};
			\node[layer,fill=boxgray,below right=of back,xshift=0.5cm] (chroma) {\textbf{ChromaDB}\\Vecteurs RAG};
			\node[layer,fill=darknavy!15,draw=darknavy,right=of back] (mistral) {\textbf{Mistral AI}\\LLM externe};
			\draw[-{Latex[length=2mm]}] (front) -- node[right]{\small HTTPS/JWT} (back);
			\draw[-{Latex[length=2mm]}] (back) -- (db);
			\draw[-{Latex[length=2mm]}] (back) -- (minio);
			\draw[-{Latex[length=2mm]}] (back) -- (chroma);
			\draw[-{Latex[length=2mm]},dashed] (back) -- (mistral);
		\end{tikzpicture}
		\caption{Architecture simplifi\'ee de la solution cible}
		\label{fig:archi-cible}
	\end{figure}
	
	\section{Objectifs du projet}
	
	Le projet GRC AI Analyzer poursuit plusieurs objectifs visant \`a automatiser et faciliter l'analyse documentaire GRC.
	
	\begin{enumerate}[leftmargin=1.8em,itemsep=6pt]
		
		\item \textbf{Automatiser l'extraction des risques.}\\
		La plateforme doit analyser des documents (PDF, DOCX, XLSX) afin d'identifier automatiquement les risques et de r\'eduire le temps d'analyse.
		
		\item \textbf{\'Evaluer la conformit\'e multi-r\'ef\'erentiels.}\\
		La solution doit d\'etecter les \'ecarts de conformit\'e par rapport \`a ISO~27001, au RGPD et \`a la Loi~09-08, avec une tra\c{c}abilit\'e claire des contr\^oles concern\'es.
		
		\item \textbf{G\'en\'erer des recommandations contextuelles.}\\
		La plateforme doit proposer des recommandations adapt\'ees aux risques identifi\'es et prioris\'ees selon leur criticit\'e.
		
		\item \textbf{Fournir une interface conversationnelle (RAG).}\\
		Un module RAG doit permettre d'interroger les rapports en langage naturel pour obtenir des explications, synth\`eses ou informations cibl\'ees.
		
		\item \textbf{Permettre l'export des r\'esultats.}\\
		Les analyses doivent pouvoir \^etre export\'ees sous forme de rapports PDF et de tableaux de bord Excel exploitables en contexte professionnel.
		
	\end{enumerate}
	
	\begin{tcolorbox}[colback=darknavy!5,colframe=darknavy,arc=2pt,boxrule=0.6pt]
		\small\textbf{Convergence des objectifs.} Ces cinq objectifs forment un pipeline coh\'erent~: l'extraction alimente l'\'evaluation, qui fonde les recommandations, que l'interface conversationnelle rend explorable, et que l'export mat\'erialise en livrables. Chaque composant de la plateforme correspond directement \`a l'un de ces objectifs.
	\end{tcolorbox}
	
	\section{Conduite du projet}
	
	La conduite du projet s'appuie sur une d\'emarche it\'erative et incr\'ementale, organis\'ee autour de quatre grandes phases de d\'eveloppement, depuis la mise en place du socle technique jusqu'\`a la livraison des fonctionnalit\'es avanc\'ees d'export et de supervision.
	
	\begin{figure}[ht]
		\centering
		\begin{tikzpicture}[node distance=0.4cm]
			\tikzstyle{phase}=[rectangle,draw=darknavy,fill=lightgray,thick,minimum width=3.2cm,minimum height=1.0cm,align=center,font=\small,rounded corners=2pt]
			\node[phase] (p1) {Phase 1\\Socle \& Auth};
			\node[phase,right=of p1] (p2) {Phase 2\\Ingestion};
			\node[phase,right=of p2] (p3) {Phase 3\\Pipeline IA};
			\node[phase,right=of p3] (p4) {Phase 4\\RAG \& Export};
			\draw[-{Latex[length=2mm]},darknavy,thick] (p1) -- (p2);
			\draw[-{Latex[length=2mm]},darknavy,thick] (p2) -- (p3);
			\draw[-{Latex[length=2mm]},darknavy,thick] (p3) -- (p4);
		\end{tikzpicture}
		\caption{Phases de d\'eveloppement du projet GRC AI Analyzer}
		\label{fig:phases}
	\end{figure}
	
	\section{Synth\`ese du chapitre}
	Ce chapitre a permis de cadrer le projet dans son environnement professionnel et r\'eglementaire. L'\'etude du domaine GRC a mis en \'evidence la n\'ecessit\'e d'une solution intelligente, automatis\'ee et adapt\'ee au contexte marocain. La probl\'ematique est clairement identifi\'ee et les objectifs sont d\'efinis. Le chapitre suivant dresse l'\'etat de l'art des solutions existantes et positionne GRC AI Analyzer par rapport \`a la litt\'erature et au march\'e.
	
	% =============================================================================
	%  CHAPITRE 2 — ÉTAT DE L'ART ET ÉTUDE DE L'EXISTANT
	% =============================================================================
	\chapter{\'Etat de l'art et \'etude de l'existant}
	\label{chap:etatdelart}
	
	\begin{tcolorbox}[infobox]
		\itshape Ce chapitre dresse l'\'etat de l'art des technologies et solutions
		pertinentes pour l'analyse GRC automatis\'ee. Il analyse les pratiques actuelles,
		les limites des outils existants, les apports des LLM et du RAG, afin de
		positionner la valeur ajout\'ee de GRC AI Analyzer.
	\end{tcolorbox}
	
	% =============================================================================
	\section{Contexte~: la conformit\'e GRC, un enjeu croissant}
	% =============================================================================
	
	La gouvernance, la gestion des risques et la conformit\'e (GRC) constituent
	aujourd'hui un pilier strat\'egique pour toute organisation traitant des
	donn\'ees sensibles. La multiplication des cadres r\'eglementaires ---
	ISO~27001 au niveau international, RGPD en Europe, Loi~09-08 au Maroc ---
	impose aux entreprises une obligation de surveillance et de documentation
	continue de leur posture de s\'ecurit\'e.
	
	Or, la production de rapports GRC explose~: audits de s\'ecurit\'e,
	rapports de tests d'intrusion, analyses d'impact sur la vie priv\'ee (AIPD),
	registres des risques, politiques de s\'ecurit\'e... Chaque document peut
	contenir des dizaines de constats, de vuln\'erabilit\'es et d'\'ecarts de
	conformit\'e qu'il faut identifier, classer et traiter. Face \`a ce volume
	croissant, les processus manuels atteignent leurs limites.
	
	% =============================================================================
	\section{Pratiques actuelles d'analyse des rapports GRC}
	% =============================================================================
	
	L'analyse d'un rapport GRC est aujourd'hui un processus enti\`erement manuel,
	confié \`a un consultant senior. Il se d\'ecompose en six \'etapes s\'equentielles~:
	
	\begin{enumerate}[leftmargin=1.5em,itemsep=6pt]
		\item \textbf{R\'eception du rapport~:} le document arrive sous forme de
		fichier PDF, DOCX ou scan num\'eris\'e, parfois sans couche texte
		(OCR n\'ecessaire).
		\item \textbf{Lecture int\'egrale~:} un expert parcourt l'int\'egralit\'e
		du document --- souvent 50 \`a 200 pages --- pour former un premier
		jugement global avant toute annotation.
		\item \textbf{Identification et surlignage des risques~:} le consultant
		annote manuellement les passages suspects. Cette \'etape est sujette
		\`a des oublis et d\'epend fortement de l'exp\'erience de l'auditeur.
		\item \textbf{Retranscription dans un tableur~:} les \'el\'ements identifi\'es
		sont saisis manuellement dans un mod\`ele Excel ou Word. Toute erreur
		de saisie se propage silencieusement dans le rapport final.
		\item \textbf{Mise en correspondance avec les r\'ef\'erentiels~:} le consultant
		compare chaque constat avec les contr\^oles ISO~27001, les articles RGPD
		et --- rarement --- la Loi~09-08, faute de grilles de lecture standardis\'ees.
		\item \textbf{R\'edaction du rapport de synth\`ese~:} production du livrable
		final consolid\'e, souvent \`a partir de mod\`eles Word recycl\'es d'un
		client \`a l'autre, au risque d'introduire des informations erron\'ees.
	\end{enumerate}
	
	Ce processus mobilise un consultant senior pendant \textbf{5 \`a 10 jours} par
	rapport, ce qui repr\'esente un co\^ut significatif et limite la cadence d'analyse
	possible. Les principales limites identifi\'ees sont illustr\'ees ci-dessous~:
	
	\begin{table}[H]
		\centering
		\begin{tabularx}{\textwidth}{@{}>{\bfseries\color{titleblue}}l X@{}}
			\toprule
			\headrow \colhead{Limite} & \colhead{Description} \\
			\midrule
			Temps & 5 \`a 10 jours de travail consultant par rapport analys\'e \\
			\rowcolor{rowalt} Co\^ut & Mobilisation de profils seniors \`a haute valeur ajout\'ee \\
			Exhaustivit\'e & Risque d'oubli ou de mauvaise interpr\'etation des passages techniques \\
			\rowcolor{rowalt} Subjectivit\'e & Variabilit\'e des conclusions entre auditeurs diff\'erents \\
			Scalabilit\'e & Impossibilit\'e de traiter plusieurs dizaines de rapports simultan\'ement \\
			\rowcolor{rowalt} Tra\c{c}abilit\'e & Difficult\'e \`a lier chaque constat \`a une section pr\'ecise du document source \\
			\bottomrule
		\end{tabularx}
		\caption{Limites des pratiques manuelles d'analyse GRC}
		\label{tab:limites}
	\end{table}
	
	% =============================================================================
	\section{Solutions du march\'e et leurs limites}
	% =============================================================================
	
	\subsection{Plateformes GRC commerciales (leaders du march\'e)}
	
	Les plateformes commerciales dominantes --- \textbf{RSA Archer},
	\textbf{ServiceNow GRC} et \textbf{MetricStream} --- offrent des fonctionnalit\'es
	\'etendues de gestion des risques, des audits et de la conformit\'e.
	Destin\'ees aux grandes entreprises (Fortune~500), elles partagent les
	caract\'eristiques suivantes~:
	
	\begin{itemize}[leftmargin=1.5em,itemsep=4pt]
		\item \textbf{Architecture base de donn\'ees structur\'ee~:} les risques et
		contr\^oles sont saisis manuellement dans des formulaires pr\'ed\'efinis.
		Aucune analyse automatique de documents textuels n'est propos\'ee.
		\item \textbf{Co\^uts prohibitifs~:} licences annuelles de plusieurs dizaines
		de milliers d'euros, n\'ecessitant en outre des int\'egrateurs certifi\'es
		pour le d\'eploiement et la configuration.
		\item \textbf{Rigide couverture r\'eglementaire~:} ces outils couvrent principalement
		les cadres anglo-saxons (SOX, PCI-DSS, HIPAA). La Loi marocaine~09-08 est
		absente de leurs biblioth\`eques de contr\^oles.
		\item \textbf{Temps de d\'eploiement long~:} plusieurs mois sont n\'ecessaires
		pour param\'etrer, former les \'equipes et int\'egrer les sources de donn\'ees
		existantes.
	\end{itemize}
	
	\subsection{Solutions open-source}
	
	Des outils tels qu'\textbf{Eramba}, \textbf{SimpleRisk} ou \textbf{OpenGRC}
	proposent des alternatives accessibles financi\`erement. Leurs limites sont
	toutefois significatives pour un usage professionnel~:
	
	\begin{itemize}[leftmargin=1.5em,itemsep=4pt]
		\item Interfaces utilisateur rudimentaires, peu adapt\'ees aux \'equipes
		GRC non techniques.
		\item Absence de support des r\'ef\'erentiels r\'egionaux (Loi~09-08, normes
		africaines ou arabes).
		\item Aucune capacit\'e d'analyse s\'emantique ou d'intelligence artificielle
		int\'egr\'ee~: tout reste manuel.
		\item Communaut\'es restreintes, documentation sparse, mises \`a jour irr\'eguli\`eres.
	\end{itemize}
	
	\subsection{Usages ad hoc des LLM (ChatGPT, Copilot)}
	
	L'int\'egration des mod\`eles de langage dans le domaine GRC est encore embryonnaire.
	Les usages observ\'es se limitent \`a~:
	
	\begin{itemize}[leftmargin=1.5em,itemsep=4pt]
		\item \textbf{R\'esum\'e ad hoc~:} copier-coller d'extraits de politiques dans
		ChatGPT pour obtenir un r\'esum\'e non structur\'e, sans ancrage aux
		r\'ef\'erentiels normatifs.
		\item \textbf{Copilot pour Office~:} analyse superficielle de documents Word,
		sans extraction structur\'ee des risques ni \'evaluation de conformit\'e.
		\item \textbf{Absence de tra\c{c}abilit\'e~:} les r\'eponses ne citent pas les
		sections sources du document, rendant l'audit trail impossible.
		\item \textbf{Risque d'hallucination~:} sans m\'ecanisme RAG, le LLM peut
		inventer des constats non pr\'esents dans le document.
	\end{itemize}
	
	\subsection{Tableau comparatif}
	
	\begin{table}[H]
		\centering
		\begin{tabularx}{\textwidth}{@{}X *{6}{>{\centering\arraybackslash}p{1.25cm}}@{}}
			\toprule
			\headrow \colhead{Solution} & \colhead{Analyse auto} & \colhead{Multi-r\'ef.} & \colhead{Loi 09-08} & \colhead{RAG} & \colhead{Export PDF/XLS} & \colhead{Open source} \\
			\midrule
			RSA Archer         & \cno  & \cyes & \cno  & \cno  & \cyes & \cno  \\
			\rowcolor{rowalt} ServiceNow GRC     & \cno  & \cyes & \cno  & \cno  & \cyes & \cno  \\
			MetricStream       & \cno  & \cyes & \cno  & \cno  & \cyes & \cno  \\
			\rowcolor{rowalt} Eramba             & \cno  & \cno  & \cno  & \cno  & \cno  & \cyes \\
			SimpleRisk         & \cno  & \cno  & \cno  & \cno  & \cno  & \cyes \\
			\rowcolor{rowalt} ChatGPT (ad hoc)   & \cyes & \cno  & \cno  & \cno  & \cno  & \cno  \\
			\midrule
			\rowcolor{lightgray} \textbf{GRC AI Analyzer} & \cyes & \cyes & \cyes & \cyes & \cyes & \cyes \\
			\bottomrule
		\end{tabularx}
		\caption{Comparatif des solutions GRC existantes}
		\label{tab:comparatif}
	\end{table}
	
	\begin{tcolorbox}[critiquestyle]
		\textbf{Synth\`ese.}
		Aucune solution existante ne combine simultan\'ement~:
		une analyse automatis\'ee de documents non structur\'es (PDF, DOCX, XLSX),
		une couverture multi-r\'ef\'erentiels incluant la Loi~09-08 marocaine,
		un moteur RAG pour l'interrogation conversationnelle ancr\'ee aux normes,
		et un export PDF/Excel pour l'audit trail.
		Cette lacune justifie la conception de GRC AI Analyzer.
	\end{tcolorbox}
	
	% =============================================================================
	\section{Fondements technologiques retenus}
	% =============================================================================
	
	\subsection{Les grands mod\`eles de langage (LLM)}
	
	Les LLM (\textit{Large Language Models}) sont des mod\`eles de Deep Learning
	bas\'es sur l'architecture Transformer~\cite{vaswani2017}, entra\^in\'es sur des corpus textuels
	de plusieurs dizaines de milliards de tokens. Ils d\'eveloppent une
	compr\'ehension du langage naturel suffisamment riche pour~:
	
	\begin{itemize}[leftmargin=1.5em,itemsep=4pt]
		\item \textbf{Extraire de l'information structur\'ee~:} identifier des entit\'es
		(risques, vuln\'erabilit\'es, contrôles) et les retourner dans un format
		JSON pr\'edefini, exploitable par une application.
		\item \textbf{Raisonner sur des r\'ef\'erentiels normatifs~:} \'evaluer la conformit\'e
		d'un document par rapport aux exigences d'ISO~27001 ou du RGPD, en
		produisant un score et une justification textuelle.
		\item \textbf{G\'en\'erer du contenu expert~:} r\'ediger un r\'esum\'e ex\'ecutif
		destin\'e \`a un RSSI, formuler des recommandations techniques d\'etaill\'ees
		avec r\'ef\'erences OWASP~\cite{owasp2021} et NIST~\cite{nistcsf}.
	\end{itemize}
	
	\paragraph{Pourquoi Mistral Large~?}
	Parmi les LLM disponibles via API en 2024--2025, \textbf{Mistral Large}~\cite{mistral2024}
	a \'et\'e retenu pour plusieurs raisons~:
	
	\begin{table}[H]
		\centering
		\begin{tabularx}{\textwidth}{@{}>{\bfseries\color{titleblue}}l X@{}}
			\toprule
			\headrow \colhead{Crit\`ere} & \colhead{Justification} \\
			\midrule
			Qualit\'e du fran\c{c}ais & Meilleure ma\^itrise du fran\c{c}ais technique que GPT-4o
			sur les tests internes (terminologie GRC, r\'ef\'erentiels ISO) \\
			\rowcolor{rowalt} Sorties JSON structur\'ees & Respect fiable du format JSON impos\'e par les prompts,
			r\'eduisant les erreurs de parsing \\
			Fen\^etre de contexte & 128k tokens~: permet d'analyser des documents de 80+ pages \\
			\rowcolor{rowalt} Tarification & Co\^ut API significativement inf\'erieur \`a GPT-4o pour un volume
			\'equivalent de tokens \\
			Souverainet\'e & Mod\`ele europ\'een, compatible avec les exigences RGPD de
			localisation des donn\'ees \\
			\bottomrule
		\end{tabularx}
		\caption{Justification du choix de Mistral Large}
		\label{tab:mistral}
	\end{table}
	
	\subsection{Le Retrieval Augmented Generation (RAG)}
	
	Le RAG~\cite{lewis2020rag} est une architecture hybride
	qui pallie la principale limite des LLM~: leur \textbf{m\'emoire param\'etrique fig\'ee}.
	Un LLM seul ne peut r\'epondre qu'\`a partir de ce qu'il a appris lors de son
	entra\^inement~; il ne ``conna\^it'' pas le rapport GRC soumis par l'utilisateur.
	
	Le RAG r\'esout ce probl\`eme en deux temps~:
	
	\begin{enumerate}[leftmargin=1.5em,itemsep=6pt]
		\item \textbf{Indexation (\textit{offline})~:} le document est d\'ecoup\'e en
		fragments (\textit{chunks}), chacun transform\'e en vecteur num\'erique
		(\textit{embedding}) repr\'esentant sa signification s\'emantique, puis
		stock\'e dans une base vectorielle.
		\item \textbf{Interrogation (\textit{online})~:} \`a chaque question, la requ\^ete
		est encod\'ee de la m\^eme fa\c{c}on, et les $k$ fragments les plus proches
		s\'emantiquement sont r\'ecup\'er\'es pour \^etre inject\'es comme contexte
		dans le prompt envoy\'e au LLM.
	\end{enumerate}
	
	\begin{figure}[H]
		\centering
		\resizebox{\textwidth}{!}{%
			\begin{tikzpicture}[node distance=0.5cm and 0.8cm]
				
				\tikzstyle{box}=[rectangle,draw=darknavy,thick,fill=lightgray,
				minimum width=2.2cm,minimum height=0.8cm,rounded corners=2pt,
				align=center,font=\small]
				
				\tikzstyle{boxg}=[rectangle,draw=darkgreen,thick,fill=darkgreen!12,
				minimum width=2.2cm,minimum height=0.8cm,rounded corners=2pt,
				align=center,font=\small]
				
				\tikzstyle{lbl}=[font=\footnotesize\bfseries\color{darknavy}]
				\tikzstyle{arr}=[-{Latex[length=2mm]},thick]
				
				% Indexation
				\node[lbl] (il) {Indexation};
				\node[box,right=0.3cm of il]   (doc)   {Document\\PDF/DOCX};
				\node[box,right=0.6cm of doc]  (chunk) {Chunking\\1500\,/\,200};
				\node[box,right=0.6cm of chunk](emb1)  {Embedding\\MiniLM};
				\node[box,right=0.6cm of emb1] (db)    {ChromaDB\\index HNSW};
				
				% Interrogation
				\node[lbl,below=1.6cm of il] (ql) {Interrogation};
				\node[boxg,right=0.3cm of ql]   (q)    {Question\\utilisateur};
				\node[boxg,right=0.6cm of q]    (emb2) {Embedding\\requête};
				\node[boxg,right=0.6cm of emb2] (ret)  {Top-5 chunks\\cosinus};
				\node[boxg,right=0.6cm of ret]  (llm)  {Mistral AI\\génération};
				\node[boxg,right=0.6cm of llm]  (ans)  {Réponse\\sourcée};
				
				\draw[arr] (doc)--(chunk);
				\draw[arr] (chunk)--(emb1);
				\draw[arr] (emb1)--(db);
				
				\draw[arr] (q)--(emb2);
				\draw[arr] (emb2)--(ret);
				\draw[arr] (ret)--(llm);
				\draw[arr] (llm)--(ans);
				
				\draw[arr,dashed] (db) -- node[right,font=\scriptsize\itshape]{vecteurs} (ret);
				
			\end{tikzpicture}
		}
		\caption{Architecture RAG : indexation (haut) et interrogation (bas)}
		\label{fig:rag-arch}
	\end{figure}
	
	Les b\'en\'efices du RAG dans le contexte GRC sont directs~:
	
	\begin{itemize}[leftmargin=1.5em,itemsep=4pt]
		\item \textbf{R\'eduction des hallucinations~:} le LLM r\'epond \textit{uniquement}
		\`a partir des extraits r\'ecup\'er\'es, pas de sa m\'emoire g\'en\'erale.
		\item \textbf{Tra\c{c}abilit\'e des sources~:} chaque r\'eponse peut citer les
		passages exacts du document qui la justifient.
		\item \textbf{Pas de fine-tuning n\'ecessaire~:} le m\^eme mod\`ele Mistral
		s'adapte \`a n'importe quel rapport sans r\'eentra\^inement.
		\item \textbf{Multilinguisme~:} le mod\`ele d'embedding
		\texttt{paraphrase-multilingual-MiniLM-L12-v2}~\cite{sentence_transformers} supporte 50+ langues,
		permettant d'analyser des documents en fran\c{c}ais, en arabe ou en anglais.
	\end{itemize}
	
	\subsection{Les bases vectorielles}
	
	Une base vectorielle (\textit{vector store}) indexe des repr\'esentations
	s\'emantiques denses (\textit{embeddings}) de fragments textuels et permet
	une recherche par similarit\'e en temps quasi-r\'eel. Contrairement \`a une
	recherche plein texte (\textit{full-text search}) qui cherche des mots exacts,
	la recherche vectorielle retrouve des passages \textit{s\'emantiquement proches}
	m\^eme sans correspondance lexicale.
	
	\paragraph{Pourquoi ChromaDB~?}
	\textbf{ChromaDB}~\cite{chromadb} a \'et\'e retenu parmi les alternatives (Pinecone, Weaviate,
	FAISS, Qdrant) pour les raisons suivantes~:
	
	\begin{table}[H]
		\centering
		\begin{tabularx}{\textwidth}{@{}>{\bfseries\color{titleblue}}l X@{}}
			\toprule
			\headrow \colhead{Crit\`ere} & \colhead{Avantage ChromaDB} \\
			\midrule
			D\'eploiement & Mode serveur HTTP en un seul processus, sans infrastructure Kubernetes \\
			\rowcolor{rowalt} Isolation & Collections nomm\'ees nativement~: une collection par analyse,
			suppression atomique \`a la suppression du rapport \\
			Licence & Apache~2.0, open-source, pas de quotas ni de frais d'API \\
			\rowcolor{rowalt} Int\'egration Python & SDK Python natif, compatible \texttt{asyncio.to\_thread} \\
			Algorithme & Index HNSW (\textit{Hierarchical Navigable Small World})~\cite{malkov2018hnsw},
			recherche approximative en $O(\log n)$ \\
			\bottomrule
		\end{tabularx}
		\caption{Justification du choix de ChromaDB}
		\label{tab:chromadb}
	\end{table}
	
	\subsection{Le framework FastAPI}
	
	FastAPI~\cite{fastapi} a \'et\'e retenu comme socle du backend pour sa combinaison unique
	d'asynchronisme natif, de validation automatis\'ee et de documentation g\'en\'er\'ee~:
	
	\begin{itemize}[leftmargin=1.5em,itemsep=4pt]
		\item \textbf{ASGI et \texttt{asyncio}~:} chaque requ\^ete est g\'er\'ee de
		fa\c{c}on non-bloquante. Les appels longs \`a Mistral (10--30 s) ne
		p\'enalisent pas les autres requ\^etes concurrentes.
		\item \textbf{BackgroundTask~:} le pipeline d'analyse peut \^etre d\'eclench\'e
		en arri\`ere-plan (HTTP~202 imm\'ediat), le frontend polling le statut.
		\item \textbf{Pydantic~v2~:} validation et s\'erialisation des donn\'ees
		\`a la fronti\`ere de l'API, garantissant l'int\'egrit\'e des entr\'ees
		et la normalisation des sorties.
		\item \textbf{OpenAPI auto-g\'en\'er\'e~:} la documentation Swagger
		(\texttt{/docs}) est produite automatiquement \`a partir des annotations
		Python, constituant le contrat d'interface entre frontend et backend.
	\end{itemize}
	
	% =============================================================================
	\section{Synth\`ese et positionnement de GRC AI Analyzer}
	% =============================================================================
	
	GRC AI Analyzer se positionne \`a l'intersection de trois domaines~:
	
	\begin{figure}[H]
		\centering
		\begin{tikzpicture}
			\tikzstyle{bubble}=[circle,draw=darknavy,thick,fill=lightgray,
			minimum size=3.2cm,align=center,font=\small]
			\tikzstyle{center}=[circle,draw=darkgreen,thick,fill=darkgreen!20,
			minimum size=2cm,align=center,font=\small\bfseries]
			
			\node[bubble] (grc)  at (0,2.2)    {Gestion des\\risques\\et conformit\'e\\(GRC)};
			\node[bubble] (nlp)  at (-2.2,-1.3){Traitement\\du langage\\naturel\\(LLM / NLP)};
			\node[bubble] (rag)  at ( 2.2,-1.3){Recherche\\s\'emantique\\(RAG /\\vecteurs)};
			\node[center] (core) at (0,0)      {GRC AI\\Analyzer};
			
		\end{tikzpicture}
		\caption{Positionnement de GRC AI Analyzer \`a l'intersection de trois domaines}
		\label{fig:positionnement}
	\end{figure}
	
	La combinaison de ces trois domaines produit une valeur ajout\'ee
	qui n'existe dans aucune solution actuelle~:
	
	\begin{itemize}[leftmargin=1.5em,itemsep=6pt]
		\item \textbf{GRC + LLM~:} automatisation de l'analyse de documents non
		structur\'es, extraction de risques structur\'es et \'evaluation de
		conformit\'e multi-r\'ef\'erentiels sans saisie manuelle.
		\item \textbf{GRC + RAG~:} interrogation conversationnelle d'un rapport en
		langage naturel, avec r\'eponses ancr\'ees aux passages sources du
		document~--- \'eliminant le risque d'hallucination.
		\item \textbf{LLM + RAG~:} g\'en\'eration augment\'ee~: le LLM ne s'appuie
		pas sur sa m\'emoire param\'etrique mais sur les fragments pertinents
		du rapport, rendant les r\'eponses factuelles et tra\c{c}ables.
	\end{itemize}
	
	\begin{tcolorbox}[critiquestyle]
		\textbf{Conclusion.}
		L'analyse de l'existant r\'ev\`ele une lacune claire~: aucune solution disponible
		ne combine une analyse automatique de documents GRC non structur\'es,
		une couverture des trois r\'ef\'erentiels ISO~27001 / RGPD / Loi~09-08,
		un moteur RAG conversationnel ancr\'e aux normes, et un pipeline
		open-source d\'eployable sans infrastructure complexe.
		GRC AI Analyzer a \'et\'e con\c{c}u pour combler exactement cette lacune.
	\end{tcolorbox}
	% =============================================================================
	%  CHAPITRE 3 — ANALYSE DES BESOINS ET CONCEPTION
	% =============================================================================
	\chapter{Analyse des besoins et conception}
	
	\section{Introduction}
	
	Ce chapitre traduit les exigences op\'erationnelles en artefacts de conception formels~: identification des acteurs et des besoins fonctionnels et non fonctionnels, diagramme des cas d'utilisation, diagramme de classes, diagramme de s\'equence, diagramme d'activit\'e, architecture globale en couches et diagramme de d\'eploiement. Ces mod\`eles constituent la r\'ef\'erence conceptuelle compl\`ete de toute la phase d'impl\'ementation.
	
	% ============================================================
	\section{Analyse des besoins}
	% ============================================================
	
	\subsection{Identification des acteurs}
	
	La plateforme GRC AI Analyzer implique trois acteurs principaux dont les
	périmètres d'action sont distincts :
	
	\begin{itemize}
		\item \textbf{L'Analyste GRC} — acteur central du système. Il uploade
		les rapports (PDF, DOCX, XLSX), déclenche les analyses, consulte les
		résultats (matrice des risques, taux de conformité, recommandations),
		interagit avec l'assistant RAG et exporte les livrables.
		
		\item \textbf{L'Encadrant / Manager} — profil à accès en lecture. Il
		consulte le tableau de bord, visualise le score de maturité GRC global,
		suit l'avancement des recommandations et accède aux rapports d'analyse
		générés par les analystes.
		
		\item \textbf{L'Administrateur} — responsable de la gouvernance applicative.
		Il gère les comptes utilisateurs (création, attribution des rôles
		\texttt{ADMIN} / \texttt{ANALYSTE}), consulte les journaux d'activité via l'export Excel (audit trail --- aucune table \texttt{PisteAudit} n'existe en v1) et
		peut configurer les référentiels de conformité.
		
		\item \textbf{Mistral AI} — acteur externe (API tierce). Le modèle
		\texttt{mistral-large-latest} est sollicité pour l'extraction des
		risques, les vérifications de conformité, la génération des
		recommandations et la production du résumé exécutif.
	\end{itemize}
	
	\subsection{Besoins fonctionnels}
	
	Les besoins fonctionnels ont été organisés en cinq modules cohérents :
	
	\subsubsection*{Module 1 — Gestion des rapports}
	
	\begin{itemize}
		\item \textbf{BF-01} : L'analyste doit pouvoir uploader un fichier au
		format PDF, DOCX ou XLSX dont la taille ne dépasse pas la limite
		configurée. Le système valide l'extension et rejette tout format non
		supporté.
		\item \textbf{BF-02} : Le système enregistre les métadonnées du rapport
		(nom, format, date d'upload, statut) dans la base de données et stocke
		le fichier binaire dans le stockage objet \textbf{MinIO} (bucket \texttt{grc-reports}).
		\item \textbf{BF-03} : L'analyste peut consulter l'historique de ses
		rapports et filtrer par statut (\textit{en\_attente}, \textit{en\_cours},
		\textit{termine}, \textit{erreur}).
		\item \textbf{BF-04} : L'analyste peut supprimer un rapport, ce qui
		entraîne la suppression en cascade des analyses associées, des fichiers
		physiques et des collections vectorielles ChromaDB correspondantes.
	\end{itemize}
	
	\subsubsection*{Module 2 — Analyse IA automatisée}
	
	\begin{itemize}
		\item \textbf{BF-05} : À partir d'un rapport uploadé, l'analyste peut
		lancer une analyse automatique. L'API répond immédiatement (HTTP 202)
		et l'analyse s'exécute en tâche de fond.
		\item \textbf{BF-06} : Le système extrait le texte brut du document
		(page par page pour PDF via PyMuPDF, paragraphe par paragraphe pour
		DOCX via python-docx, feuille par feuille pour XLSX via openpyxl).
		\item \textbf{BF-07} : Le système découpe le texte en fragments
		(\textit{chunks}) de 1 500 caractères avec un chevauchement de 200
		caractères, puis les indexe dans ChromaDB via le modèle d'embedding
		multilingue \texttt{paraphrase-multilingual-MiniLM-L12-v2}.
		\item \textbf{BF-08} : Le système extrait les risques de manière
		exhaustive : chaque alerte, vulnérabilité, écart ou finding du document
		constitue un risque distinct, caractérisé par un libellé, une
		description technique, une catégorie (\textit{CYBER, OPERATIONNEL,
			LEGAL, FINANCIER, RH}), une probabilité (1-5), un impact (1-5) et
		un score calculé (probabilité × impact).
		\item \textbf{BF-09} : La sévérité est déterminée automatiquement selon
		les seuils : score $\geq$ 20 → CRITIQUE ; $\geq$ 12 → ÉLEVÉ ;
		$\geq$ 6 → MOYEN ; $<$ 6 → FAIBLE.
		\item \textbf{BF-10} : Le système vérifie la conformité du document par
		rapport à trois référentiels en parallèle : ISO~27001:2022 (14~domaines de l'Annexe~A selon la structure \textbf{2013 adopt\'ee par le prompt}, couvrant les 93~contr\'oles r\'eels de la version 2022 par regroupement), RGPD (12~domaines), Loi~09-08 marocaine (11~domaines).
		Chaque domaine reçoit un statut (CONFORME / PARTIEL / NON\_CONFORME)
		et un taux de conformité.
		\item \textbf{BF-11} : Le système génère une recommandation priorisée
		pour chaque risque identifié, incluant un type d'action
		(\textit{QUICK\_WIN} ou \textit{LONG\_TERME}) et un effort estimé
		(\textit{FAIBLE, MOYEN, ÉLEVÉ}).
		\item \textbf{BF-12} : Le système produit un résumé exécutif structuré
		(350-450 mots), destiné à un CISO, synthétisant les conclusions
		clés, les risques prioritaires, l'état de conformité et les actions
		immédiates recommandées.
		\item \textbf{BF-13} : Le score de maturité GRC global est calculé comme
		la moyenne arithmétique des taux de conformité des trois référentiels.
	\end{itemize}
	
	\subsubsection*{Module 3 — Consultation et visualisation}
	
	\begin{itemize}
		\item \textbf{BF-14} : L'analyste peut consulter la matrice des risques
		(visualisation Probabilité × Impact) via un composant Recharts dédié.
		\item \textbf{BF-15} : L'analyste peut visualiser le taux de conformité
		par référentiel via un radar chart (composant \texttt{ComplianceRadar}).
		\item \textbf{BF-16} : L'analyste peut lire les recommandations classées
		par priorité (CRITIQUE → HAUTE → MOYENNE → FAIBLE) et mettre à jour
		leur statut de traitement (\textit{À\_FAIRE} → \textit{EN\_COURS}
		→ \textit{CLÔTURÉ}).
		\item \textbf{BF-17} : Le tableau de bord affiche un score de maturité
		global, le nombre de risques par sévérité et les taux de conformité
		de la dernière analyse.
	\end{itemize}
	
	\subsubsection*{Module 4 — Assistant RAG conversationnel}
	
	\begin{itemize}
		\item \textbf{BF-18} : L'analyste peut poser des questions en langage
		naturel (français ou anglais) sur le contenu du rapport. Le système
		récupère les 5 fragments les plus pertinents dans ChromaDB par
		similarité cosinus, construit un contexte et interroge Mistral pour
		générer une réponse sourcée.
		\item \textbf{BF-19} : Les réponses du chat citent explicitement les
		extraits du rapport sur lesquels elles s'appuient.
	\end{itemize}
	
	\subsubsection*{Module 5 — Export}
	
	\begin{itemize}
		\item \textbf{BF-20} : L'analyste peut exporter l'analyse complète en
		PDF (rapport structuré avec page de couverture, résumé exécutif,
		tableau des risques, résultats de conformité et recommandations),
		généré via ReportLab.
		\item \textbf{BF-21} : L'analyste peut exporter le registre des risques
		en fichier Excel (.xlsx) avec quatre onglets (Résumé, Risques,
		Conformité, Recommandations), généré via XlsxWriter avec mise en
		forme conditionnelle par sévérité.
	\end{itemize}
	
	\subsubsection*{Module 6 — Authentification et administration}
	
	\begin{itemize}
		\item \textbf{BF-22} : Le système implémente une authentification par
		JWT (JSON Web Token). Le token est signé avec l'algorithme HS256,
		stocké côté client dans le \textit{localStorage} via Zustand, et
		transmis dans l'en-tête \texttt{Authorization: Bearer} à chaque
		requête. \textit{(Note~: le stockage en \texttt{localStorage} expose le token
			à une attaque XSS~; un cookie \texttt{httpOnly} aurait été plus sûr~;
			ce compromis est assumé dans le cadre de ce prototype.)}
		\item \textbf{BF-23} : Les mots de passe sont hachés avec bcrypt avant
		stockage. La vérification à la connexion compare le hash stocké sans
		jamais manipuler le mot de passe en clair.
		\item \textbf{BF-24} : L'administrateur peut créer et gérer les
		utilisateurs, leur attribuer le rôle \texttt{ADMIN} ou
		\texttt{ANALYSTE}. Un analyste ne peut accéder qu'à ses propres
		rapports ; un administrateur a une vue globale.
	\end{itemize}
	
	\subsection{Besoins non fonctionnels}
	
	\subsubsection*{Performance et scalabilité}
	
	\begin{itemize}
		\item \textbf{BNF-01} : L'API doit répondre aux requêtes de consultation
		en moins de 500 ms en conditions normales. L'upload et le déclenchement
		de l'analyse retournent immédiatement (HTTP 202) sans bloquer le
		client.
		\item \textbf{BNF-02} : Le pipeline d'analyse exploite la concurrence
		via \texttt{asyncio.gather} : l'indexation ChromaDB, l'extraction
		des risques et les trois vérifications de conformité s'exécutent en
		parallèle (Phase 1) ; la génération des recommandations et du résumé
		exécutif s'exécutent elles aussi en parallèle (Phase 2).
		\item \textbf{BNF-03} : Les appels à l'API Mistral sont sérialisés par
		un sémaphore global (\texttt{asyncio.Semaphore(1)}) avec un délai
		minimum de 1,5 seconde entre appels consécutifs pour respecter les
		limites de débit (\textit{rate limits}). Un mécanisme de backoff
		exponentiel gère les erreurs HTTP 429 (délai de 30 s × numéro
		de tentative).
	\end{itemize}
	
	\subsubsection*{Fiabilité et résilience}
	
	\begin{itemize}
		\item \textbf{BNF-04} : L'indexation RAG dans ChromaDB est
		\textit{non-bloquante} : un échec de ChromaDB n'interrompt pas
		l'analyse, la plateforme continue sans fonctionnalité de chat.
		\item \textbf{BNF-05} : En cas d'erreur dans le pipeline, le statut
		de l'analyse est mis à jour en \textit{erreur} dans la base de
		données et l'erreur est journalisée via Loguru, sans lever
		d'exception non gérée.
		\item \textbf{BNF-06} : Toutes les sessions de base de données du
		pipeline asynchrone s'exécutent dans un contexte transactionnel
		dédié (\texttt{AsyncSessionLocal}), distinct de la session HTTP,
		pour éviter tout conflit de concurrence.
	\end{itemize}
	
	\subsubsection*{Sécurité}
	
	\begin{itemize}
		\item \textbf{BNF-07} : Toutes les routes API (sauf \texttt{/auth/login}
		et \texttt{/auth/register}) sont protégées par la dépendance
		\texttt{get\_current\_user} qui vérifie la validité du JWT.
		\item \textbf{BNF-08} : Les tokens JWT ont une durée de vie de 480
		minutes (8 heures). Un intercepteur Axios côté frontend invalide la
		session locale dès réception d'une réponse HTTP 401.
		\item \textbf{BNF-09} : La politique CORS est configurée explicitement
		pour n'autoriser que l'origine du frontend (\texttt{localhost:5173}
		en développement, URL configurable en production).
	\end{itemize}
	
	\subsubsection*{Maintenabilité et extensibilité}
	
	\begin{itemize}
		\item \textbf{BNF-10} : L'architecture adopte une séparation stricte
		des responsabilités : les routes FastAPI sont de simples façades,
		toute la logique métier réside dans la couche \texttt{services/},
		et la logique IA dans la couche \texttt{ai/}.
		\item \textbf{BNF-11} : Les migrations de base de données sont gérées
		par Alembic, permettant des évolutions du schéma sans perte de données.
		\item \textbf{BNF-12} : Les prompts Mistral sont externalisés dans des
		modules dédiés (\texttt{risk\_prompts.py}, \texttt{compliance\_prompts.py},
		\texttt{recommendation\_prompts.py}, \texttt{summary\_prompts.py}),
		facilitant leur maintenance et leur amélioration indépendante.
	\end{itemize}
	
	\subsubsection*{Internationalisation}
	
	\begin{itemize}
		\item \textbf{BNF-13} : Le modèle d'embedding
		\texttt{paraphrase-multilingual-MiniLM-L12-v2} supporte 50+ langues,
		permettant l'analyse de documents en français, anglais ou arabe.
		L'assistant RAG répond dans la langue de la question posée.
	\end{itemize}
	
	% ============================================================
	\section{Diagramme des Cas d'Utilisation}
	% ============================================================
	
	\subsection{Description générale}
	
	Le diagramme des cas d'utilisation (figure~\ref{fig:use_case}) représente
	l'ensemble des interactions fonctionnelles entre les acteurs et le système
	GRC AI Analyzer. Il s'organise en six packages fonctionnels qui correspondent
	aux modules identifiés lors de l'analyse des besoins.
	
	\begin{figure}[H]
		\centering
		\includegraphics[width=0.82\textwidth]{figures/useC.png}
		\caption{Diagramme des cas d'utilisation — GRC AI Analyzer}
		\label{fig:use_case}
	\end{figure}
	
	\subsection{Description des cas d'utilisation principaux}
	
	\subsubsection*{UC-01 : Uploader un rapport}
	
	\begin{tcolorbox}[colback=lightgray!20, colframe=darknavy, title=UC-01 — Uploader un rapport]
		\textbf{Acteur principal :} Analyste GRC \\
		\textbf{Précondition :} L'analyste est authentifié. \\
		\textbf{Scénario nominal :}
		\begin{enumerate}
			\item L'analyste sélectionne un fichier (PDF, DOCX ou XLSX) via l'interface.
			\item Le système valide le format (\texttt{validate\_file\_format}).
			\item Le fichier est envoy\'e dans \textbf{MinIO} sous le chemin
			\texttt{reports/\{uuid\}/\{nom\_sanitisé\}} (bucket \texttt{grc-reports}).
			\item Un enregistrement \texttt{Rapport} est créé en base avec le statut
			\textit{en\_attente}.
			\item L'API retourne l'identifiant du rapport (HTTP 201).
		\end{enumerate}
		\textbf{Exception :} Format non supporté → HTTP 400 avec message d'erreur.
	\end{tcolorbox}
	
	\subsubsection*{UC-02 : Lancer l'analyse automatique}
	
	\begin{tcolorbox}[colback=lightgray!20, colframe=darknavy, title=UC-02 — Lancer l'analyse automatique]
		\textbf{Acteur principal :} Analyste GRC \\
		\textbf{Acteur secondaire :} Mistral AI \\
		\textbf{Précondition :} Un rapport au statut \textit{en\_attente} existe. \\
		\textbf{Scénario nominal :}
		\begin{enumerate}
			\item L'analyste déclenche l'analyse via
			\texttt{POST /api/v1/analyses/lancer/\{rapport\_id\}}.
			\item Le système crée un enregistrement \texttt{Analyse} (statut
			\textit{en\_cours}) et retourne immédiatement HTTP 202.
			\item En tâche de fond : extraction du texte, chunking, indexation
			ChromaDB, extraction des risques et vérifications de conformité
			(Phase 1 parallèle), puis recommandations et résumé (Phase 2
			parallèle).
			\item L'analyse est marquée \textit{terminé} à la fin du pipeline.
		\end{enumerate}
		\textbf{Cas inclus :} Extraire les risques identifiés ; Scorer les risques ;
		Vérifier la conformité ; Analyser les écarts (Gap Analysis) ;
		Générer le résumé exécutif.
	\end{tcolorbox}
	
	\subsubsection*{UC-03 : Poser des questions via le Chat IA}
	
	\begin{tcolorbox}[colback=lightgray!20, colframe=darknavy, title=UC-03 — Poser des questions sur le rapport (RAG Chat)]
		\textbf{Acteur principal :} Analyste GRC \\
		\textbf{Acteur secondaire :} Mistral AI \\
		\textbf{Précondition :} L'analyse est terminée et les chunks ont été indexés. \\
		\textbf{Scénario nominal :}
		\begin{enumerate}
			\item L'analyste saisit une question dans l'onglet Assistant RAG.
			\item Le système encode la question via le modèle sentence-transformer.
			\item ChromaDB retourne les 5 chunks les plus proches par similarité cosinus.
			\item Le système construit le prompt contextuel et interroge Mistral.
			\item La réponse est affichée avec les extraits sources cités.
		\end{enumerate}
		\textbf{Cas inclus :} Obtenir des réponses citant les sources.
	\end{tcolorbox}
	
	\subsubsection*{UC-04 : Exporter les livrables}
	
	L'analyste peut exporter deux types de livrables depuis l'interface :
	\begin{itemize}
		\item \textbf{Export PDF} — rapport d'analyse complet généré par
		ReportLab, comprenant une page de couverture avec score de maturité,
		un résumé exécutif, un tableau des risques avec badges de sévérité
		colorés, les résultats de conformité par référentiel et le plan de
		recommandations.
		\item \textbf{Export Excel} — registre des risques au format XLSX
		(XlsxWriter) avec quatre onglets structurés et mise en forme
		conditionnelle par sévérité et priorité.
	\end{itemize}
	
	\subsubsection*{UC-05 : Suivre l'avancement des recommandations}
	
	L'analyste peut faire progresser le statut de chaque recommandation
	via l'interface (bouton de cycle d'état) :
	\textit{À\_FAIRE} $\rightarrow$ \textit{EN\_COURS} $\rightarrow$
	\textit{CLÔTURÉ}, en appelant
	\texttt{PATCH /api/v1/recommandations/\{id\}/statut}.
	
	% ============================================================
	\section{Diagramme de Classes}
	% ============================================================
	
	\subsection{Vue d'ensemble}
	
	Le diagramme de classes (figure~\ref{fig:class_diagram}) modélise les
	entités persistantes du domaine et leurs relations. Il reflète fidèlement
	le schéma de base de données PostgreSQL géré par SQLAlchemy (ORM asynchrone)
	et versionné via Alembic.
	
	\begin{figure}[H]
		\centering
		\includegraphics[width=1\textwidth]{figures/class.png}
		\caption{Diagramme de classes — modèle du domaine GRC AI Analyzer}
		\label{fig:class_diagram}
	\end{figure}
	
	\subsection{Description des classes principales}
	
	\subsubsection*{Utilisateur}
	
	La classe \texttt{Utilisateur} représente les comptes de la plateforme.
	Chaque utilisateur possède un identifiant UUID, un nom, un email unique
	(indexé), un mot de passe haché (bcrypt), un rôle énuméré
	(\texttt{ADMIN} ou \texttt{ANALYSTE}) et une date de création.
	Un utilisateur peut uploader plusieurs rapports et déclencher plusieurs
	analyses (relations \textit{one-to-many}).
	
	\subsubsection*{Rapport}
	
	La classe \texttt{Rapport} encapsule les métadonnées d'un document soumis
	à analyse. Elle stocke le nom du fichier, le format (\texttt{pdf},
	\texttt{docx}, \texttt{xlsx}), le chemin de stockage sur disque (chaîne
	de type \texttt{reports/\{uuid\}/\{nom\}}), et le statut du cycle de vie
	(\textit{en\_attente} → \textit{en\_cours} → \textit{terminé} ou
	\textit{erreur}). La suppression d'un rapport entraîne la suppression
	en cascade de toutes ses analyses associées
	(\texttt{cascade="all, delete-orphan"}).
	
	\subsubsection*{Analyse}
	
	La classe \texttt{Analyse} constitue le cœur du modèle. Elle est liée à
	un rapport (many-to-one) et à un utilisateur déclencheur. Elle porte le
	résumé exécutif (texte libre), le score de maturité GRC (flottant entre
	0 et 100), et le statut de traitement. Elle agrège en composition les
	risques, les résultats de conformité et les recommandations.
	
	\subsubsection*{Risque}
	
	La classe \texttt{Risque} représente une menace, vulnérabilité ou écart
	identifié par Mistral dans le document. Ses attributs clés sont :
	\begin{itemize}
		\item \texttt{libelle} — intitulé du risque (500 caractères max)
		\item \texttt{categorie} — taxonomie en cinq valeurs : CYBER,
		OPERATIONNEL, LEGAL, FINANCIER, RH
		\item \texttt{probabilite} et \texttt{impact} — entiers de 1 à 5
		\item \texttt{score\_risque} — produit probabilité × impact (flottant)
		\item \texttt{severite} — calculée automatiquement selon les seuils
		définis dans \texttt{compute\_severity()}
		\item \texttt{section\_source} — référence à l'identifiant ou à la
		section du document source (ex. : \og{}Finding \#3\fg{},
		\og{}Section A.12\fg{})
	\end{itemize}
	
	\subsubsection*{ResultatConformite}
	
	La classe \texttt{ResultatConformite} enregistre l'évaluation d'un domaine
	de conformité pour un référentiel donné. Ses attributs principaux sont
	le référentiel (\texttt{ISO27001}, \texttt{RGPD}, \texttt{LOI0908}), le
	nom du domaine évalué, le statut (\texttt{CONFORME} / \texttt{NON\_CONFORME}
	/ \texttt{PARTIEL}), l'écart constaté (texte libre) et le taux de
	conformité (flottant). Une analyse peut produire jusqu'à 37 enregistrements
	de conformit\'e (14~domaines \'evalu\'es de l'Annexe~A ISO + 12~domaines RGPD + 11~domaines Loi~09-08 = 37~enregistrements par analyse~; ces 14~domaines correspondent \`a la grille 2013 qui couvre par regroupement les 93~contr\^oles de la version 2022).
	
	\subsubsection*{Recommandation}
	
	La classe \texttt{Recommandation} modélise une action corrective générée
	par Mistral en réponse à un risque. Elle est liée optionnellement à un
	risque (\texttt{risque\_id} nullable), permettant la traçabilité
	risque-recommandation. Ses attributs incluent : la priorité (CRITIQUE /
	HAUTE / MOYENNE / FAIBLE), le type d'action (QUICK\_WIN ou LONG\_TERME),
	l'effort estimé (FAIBLE / MOYEN / ÉLEVÉ) et le statut de traitement
	(À\_FAIRE / EN\_COURS / CLÔTURÉ).
	
	\subsubsection*{MessageChat \textnormal{\small(classe conceptuelle, non persist\'ee en base)}}
	
	La classe \texttt{MessageChat} est pr\'esente dans le mod\`ele conceptuel UML
	mais n'est \textbf{pas persist\'ee} dans PostgreSQL~: les messages du chat sont
	retourn\'es directement en r\'eponse API sans \'ecriture en base, pour des raisons
	de simplicit\'e et de performance. Elle stocke conceptuellement la question
	de l'utilisateur, la r\'eponse Mistral et les sources ChromaDB cit\'ees.
	
	\subsubsection*{ReferentielGRC \textnormal{\small(classe conceptuelle, cod\'ee en dur)}}
	
	La classe \texttt{ReferentielGRC} est une \textbf{abstraction conceptuelle}~:
	les r\'ef\'erentiels (ISO~27001, RGPD, Loi~09-08) sont cod\'es en dur dans les
	prompts IA (\texttt{compliance\_prompts.py}) et ne n\'ecessitent pas de table
	PostgreSQL d\'edi\'ee. Elle figure dans le diagramme de classes pour formaliser
	la relation entre une analyse et ses r\'ef\'erentiels \'evalu\'es.
	
	\subsubsection*{PisteAudit \textnormal{\small(classe conceptuelle, non impl\'ement\'ee v1)}}
	
	La classe \texttt{PisteAudit} est \textbf{pr\'evue pour une version future}~:
	elle enregistrerait les actions sensibles (upload, lancement d'analyse,
	modification de statut) pour l'audit trail administrateur. Dans la version~1
	actuelle, cette fonctionnalit\'e n'est pas encore impl\'ement\'ee en base PostgreSQL.
	Le diagramme de classes la conserve comme exigence de conception \`a satisfaire
	lors d'une prochaine it\'eration.
	
	% ============================================================
	\section{Diagramme de Séquence}
	% ============================================================
	
	\subsection{Séquence principale : Analyse d'un rapport GRC}
	
	Le diagramme de séquence (figure~\ref{fig:sequence_diagram}) décrit les
	interactions chronologiques entre les composants lors de l'opération la
	plus complexe du système : le lancement et l'exécution complète d'une
	analyse IA.
	
	\begin{figure}[H]
		\centering
		\includegraphics[width=\textwidth]{figures/seq.png}
		\caption{Diagramme de séquence — Analyse d'un rapport GRC}
		\label{fig:sequence_diagram}
	\end{figure}
	
	\subsection{Description détaillée des interactions}
	
	Le flux se décompose en deux grandes phases, séparées par une frontière
	asynchrone matérialisée par la réponse HTTP 202.
	
	\subsubsection*{Phase synchrone : Upload et déclenchement}
	
	\begin{enumerate}
		\item L'analyste soumet son fichier depuis le frontend React via
		\texttt{POST /api/rapports/upload}. Axios joint le token JWT dans
		l'en-tête de la requête.
		\item Le backend FastAPI valide le format du fichier et délègue au
		\texttt{report\_service} qui sauvegarde le fichier sur disque et
		crée l'enregistrement \texttt{Rapport} en base de données
		(statut : \textit{en\_attente}).
		\item Le frontend reçoit le \texttt{rapport\_id} et affiche une
		confirmation d'upload.
		\item L'analyste déclenche l'analyse via
		\texttt{POST /api/analyses/lancer/\{rapport\_id\}}.
		\item Le backend crée immédiatement l'enregistrement \texttt{Analyse}
		(statut : \textit{en\_cours}), enregistre la tâche de fond via
		\texttt{BackgroundTasks}, et retourne HTTP 202 avec
		l'\texttt{analyse\_id}.
	\end{enumerate}
	
	\subsubsection*{Pipeline IA asynchrone~: 6~appels LLM, 4~prompts, 2~phases}
	
	Le service \texttt{analysis\_service.run\_analysis()} s'exécute dans
	une session SQLAlchemy dédiée. Il suit le pipeline suivant :
	
	\begin{enumerate}
		\item \textbf{Extraction du texte} — le fichier est lu depuis le disque
		et parsé selon son format. Le texte brut est découpé en chunks.
		
		\item \textbf{Phase 1 — Parallélisation via \texttt{asyncio.gather}} :
		\begin{itemize}
			\item Indexation ChromaDB (embedding des chunks en mémoire --- non-LLM)
			\item \textbf{Appel LLM 1} $\to$ Mistral : extraction exhaustive des risques (JSON structur\'e)
			\item \textbf{Appel LLM 2} $\to$ Mistral : v\'erification conformit\'e ISO~27001 (14 domaines Annexe~A)
			\item \textbf{Appel LLM 3} $\to$ Mistral : v\'erification conformit\'e RGPD (12 domaines)
			\item \textbf{Appel LLM 4} $\to$ Mistral : v\'erification conformit\'e Loi~09-08 (11 domaines)
		\end{itemize}
		Le scoring probabilit\'e $\times$ impact et le calcul de s\'ev\'erit\'e sont
		\textbf{d\'eterministes} (formule $p \times i$, seuils fix\'es) --- pas d'appel LLM.
		Les r\'esultats sont valid\'es et persist\'es en base de donn\'ees.
		
		\item \textbf{Gap Analysis consolid\'ee} --- le score de maturit\'e global
		est calcul\'e de fa\c{c}on \textbf{arithm\'etique}~:
		$\overline{t} = (t_{\text{ISO}} + t_{\text{RGPD}} + t_{\text{Loi}}) / 3$.
		Aucun appel LLM.
		
		\item \textbf{Phase 2 --- Parall\'elisation via \texttt{asyncio.gather}} :
		\begin{itemize}
			\item \textbf{Appel LLM 5} $\to$ Mistral : g\'en\'eration des recommandations prioris\'ees
			(une par risque identifi\'e)
			\item \textbf{Appel LLM 6} $\to$ Mistral : g\'en\'eration du r\'esum\'e ex\'ecutif
			(350--450 mots, structur\'e en 6 sections)
		\end{itemize}
		
		\item \textbf{Sauvegarde finale} — l'enregistrement \texttt{Analyse}
		est mis à jour avec le résumé, le score de maturité et le statut
		\textit{terminé}.
	\end{enumerate}
	
	\subsubsection*{Phase de consultation}
	
	Le frontend React interroge \texttt{GET /api/analyses/\{id\}} toutes les
	3~secondes (polling) jusqu'\`a obtenir le statut \textit{termin\'e}.
	À ce moment, trois requêtes parallèles (\texttt{Promise.all}) récupèrent
	les risques, les résultats de conformité et les recommandations, qui sont
	affichés dans les onglets correspondants de la page d'analyse.
	
	% ============================================================
	\section{Diagramme d'Activité}
	% ============================================================
	
	\subsection{Vue d'ensemble du flux de traitement}
	
	Le diagramme d'activité (figure~\ref{fig:activity_diagram}) modélise le
	flux de contrôle complet du système, de l'upload du rapport jusqu'à la
	consultation des résultats et l'export des livrables.
	\begin{figure}[H]
		\centering
		\includegraphics[width=0.68\textwidth]{figures/act.png}
		\caption{Diagramme d'activité — flux de traitement GRC AI Analyzer}
		\label{fig:activity_diagram}
	\end{figure}
	
	\subsection{Description des activités}
	
	\subsubsection*{Bloc 1 — Préparation du document}
	
	Les quatre premières activités forment une séquence linéaire :
	
	\begin{enumerate}
		\item \textbf{Upload du rapport} — l'analyste soumet son fichier
		PDF/DOCX/XLSX via l'interface. Le système valide le format,
		sauvegarde le fichier et enregistre les métadonnées.
		\item \textbf{Extraction et nettoyage du texte} — selon le format
		détecté, l'un des trois parsers est appelé (PyMuPDF pour PDF,
		python-docx pour DOCX, openpyxl pour XLSX). Le texte est extrait
		structuré par page, paragraphe ou cellule.
		\item \textbf{Découpage en chunks} — le texte est fragmenté en segments
		de 1 500 caractères avec un chevauchement de 200 caractères assurant
		la continuité sémantique.
		\item \textbf{Génération des embeddings} — chaque chunk est transformé
		en vecteur dense de 384 dimensions par le modèle
		\texttt{paraphrase-multilingual-MiniLM-L12-v2} et indexé dans
		ChromaDB dans une collection nommée
		\texttt{analyse\_\{id\}}.
	\end{enumerate}
	
	\subsubsection*{Bloc 2 — Analyse IA parallèle (Phase 1)}
	
	Un nœud de fourche (\textit{fork}) déclenche simultanément quatre
	activités parallèles, matérialisant l'utilisation de
	\texttt{asyncio.gather} dans le code :
	
	\begin{itemize}
		\item \textbf{Extraction des risques (Mistral)} — appel JSON structuré
		retournant la liste des risques avec libellé, description, catégorie,
		probabilité, impact et référence source.
		\item \textbf{Scoring probabilité × impact} — calcul déterministe du
		score (\textit{score = probabilité × impact}) et classification
		automatique de la sévérité selon les seuils définis.
		\item \textbf{Vérification ISO 27001 (Mistral)} — évaluation des
		14~domaines de la grille Annexe~A (nomenclature ISO~27001:2013, compatible 2022) avec statut CONFORME/PARTIEL/NON\_CONFORME
		et taux par domaine.
		\item \textbf{Vérification RGPD (Mistral)} — évaluation des 12 articles
		RGPD pertinents.
		\item \textbf{Vérification Loi 09-08 (Mistral)} — évaluation des
		11 domaines de la loi marocaine de protection des données.
	\end{itemize}
	
	Un nœud de jonction (\textit{join}) synchronise les cinq activit'es
	parallèles (dont 4~appels LLM~; l'indexation ChromaDB est la seule t\^ache non-LLM)
	avant de passer à la consolidation.
	
	\subsubsection*{Bloc 3 — Consolidation et génération (Phase 2)}
	
	\begin{enumerate}
		\item \textbf{Consolidation gap analysis} — les taux de conformité
		sont agrégés, le score de maturité global est calculé.
		\item \textbf{Génération des recommandations priorisées (Mistral)} et
		\textbf{génération du résumé exécutif (Mistral)} s'exécutent en
		parallèle.
		\item \textbf{Sauvegarde en base de données} — tous les objets (risques,
		conformités, recommandations, résumé) sont persistés dans PostgreSQL.
		\item \textbf{Mise à jour du tableau de bord} — le statut de l'analyse
		passe à \textit{terminé}.
	\end{enumerate}
	
	\subsubsection*{Décision : Risques critiques détectés ?}
	
	Après la mise à jour du tableau de bord, un nœud de décision vérifie
	la présence de risques de sévérité CRITIQUE (score $\geq$ 20). Si oui,
	une notification est envoyée à l'administrateur. Dans les deux cas,
	l'analyste est redirigé vers la page des résultats.
	
	\subsubsection*{Bloc 4 — Consultation des résultats (parallèle)}
	
	L'analyste dispose de quatre vues accessibles simultanément via les
	onglets de l'interface :
	\begin{itemize}
		\item \textbf{Matrice des risques} — visualisation Probabilité × Impact
		via \texttt{RiskMatrix} (Recharts).
		\item \textbf{Taux de conformité par référentiel} — radar chart
		via \texttt{ComplianceRadar} (Recharts).
		\item \textbf{Recommandations} — plan d'action priorisé avec gestion
		du cycle de vie.
		\item \textbf{Chat IA} — assistant conversationnel RAG (Mistral +
		ChromaDB).
	\end{itemize}
	
	Puis l'analyste peut suivre l'avancement des recommandations au fil
	du temps.
	
	\subsubsection*{Décision : Export nécessaire ?}
	
	Si l'analyste souhaite exporter, un nœud de fourche permet de générer
	en parallèle le rapport PDF (ReportLab) et/ou le registre Excel
	(XlsxWriter), avant de converger vers la fin du flux.
	
	% ============================================================
	\section{Architecture globale de la solution}
	% ============================================================
	
	\subsection{Vue d'ensemble en couches}
	
	La figure~\ref{fig:archi-globale} pr\'esente l'architecture globale de GRC AI Analyzer,
	organis\'ee autour de quatre grandes zones fonctionnelles qui collaborent
	pour transformer un document GRC brut en analyse structur\'ee, exploitable
	et conversationnelle.
	
	\begin{figure}[htbp]
		\centering
		\includegraphics[
		angle=90,
		width=0.4\textheight,
		keepaspectratio
		]{figures/archi.png}
		\caption{Architecture globale --- GRC AI Analyzer}
		\label{fig:archi-globale}
	\end{figure}
	
	\paragraph{Zone Frontend --- React~18 / Vite (:5173).}
	Le frontend est une Single Page Application React exposant quatre cat\'egories
	de vues~: authentification, tableau de bord, gestion des rapports et consultation
	des r\'esultats. Un \textbf{HTTP Client Axios + JWT} injecte automatiquement le
	token d'acc\`es dans chaque requ\^ete sortante. Les composants de visualisation
	(matrice des risques, radar de conformit\'e, jauge de maturit\'e, chat RAG)
	consomment les donn\'ees retourn\'ees par l'API sans logique m\'etier locale.
	
	\paragraph{Zone Backend API --- FastAPI (:8000).}
	Le backend expose un point d'entr\'ee REST unifi\'e \texttt{/api/v1/*} prot\'eg\'e
	par un \textbf{Middleware JWT + Gestion des erreurs}.
	Cinq services m\'etier distincts sont orchestr\'es depuis cette couche~:
	\textit{Chat Service} (assistant RAG),
	\textit{Report Service} (stockage et cycle de vie des rapports),
	\textit{Analysis Service} (d\'eclenchement et suivi du pipeline IA),
	\textit{Export Service} (g\'en\'eration PDF et Excel),
	et \textit{Auth Service} (inscription, connexion, gestion des r\^oles RBAC).
	
	\paragraph{Zone Moteur IA / RAG.}
	C'est le c\oe{}ur diff\'erenciant de la plateforme.
	Le flux commence par le \textbf{Document Parser} (PDF via PyMuPDF, DOCX via
	python-docx, XLSX via openpyxl), puis le \textbf{Chunking Engine} (1\,500~caract\`eres,
	chevauchement de 200) et la g\'en\'eration d'\textbf{Embeddings} par le mod\`ele
	multilingue \texttt{MiniLM-L12-v2}.
	Le \textbf{Pipeline IA Asynchrone} (\texttt{asyncio.gather()}) lance ensuite en
	parall\`ele~: l'indexation RAG (\textit{RAG Engine / Semantic Search}),
	l'extraction des risques selon ISO~27005, l'\'evaluation de la conformit\'e
	(ISO~27001, RGPD, Loi~09-08), et la g\'en\'eration des recommandations
	et du r\'esum\'e ex\'ecutif.
	Tous ces appels transitent par le \textbf{Mistral Client} dot\'e d'un
	m\'ecanisme de retry et d'un anti-429 (s\'emaphore + backoff exponentiel),
	avant d'\^etre achemin\'es vers l'\textbf{API Mistral AI} (\texttt{mistral-large-latest}).
	
	\paragraph{Zone Infrastructure.}
	Trois moteurs de persistance sp\'ecialis\'es~:
	\textbf{ChromaDB} (base vectorielle, index cosinus HNSW, une collection par analyse),
	\textbf{MinIO} (stockage objet compatible S3 pour les fichiers originaux),
	et \textbf{PostgreSQL} (m\'etadonn\'ees relationnelles et r\'esultats d'analyse via
	SQLAlchemy~2.0 asyncpg).
	Chaque service m\'etier acc\`ede directement au moteur de persistance
	qui correspond \`a la nature de ses donn\'ees.
	
	% ------------------------------------------------------------
	\subsection{Diagramme de d\'eploiement}
	% ------------------------------------------------------------
	
	Le diagramme de d\'eploiement (figure~\ref{fig:deploiement}) repr\'esente
	la distribution physique des composants logiciels sur les n\oe{}uds
	d'ex\'ecution et les protocoles de communication entre eux.
	
	\begin{figure}[htbp]
		\centering
		\makebox[\textwidth][c]{%
			\includegraphics[width=0.8\textwidth, keepaspectratio]{figures/deploi.png}%
		}
		\caption{Diagramme de d\'eploiement --- GRC AI Analyzer}
		\label{fig:deploiement}
	\end{figure}
	
	Cinq n\oe{}uds structurent le d\'eploiement de la plateforme~:
	
	\paragraph{Poste utilisateur --- Navigateur Web.}
	Le client ex\'ecute la Single Page Application React~18 + Vite.
	Les biblioth\`eques Zustand, Axios et Recharts sont charg\'ees dans le navigateur.
	L'intercepteur JWT injecte automatiquement le token dans chaque requ\^ete HTTP.
	La communication avec le backend s'effectue via \textbf{HTTP/REST + JSON + JWT}.
	
	\paragraph{Serveur d'Application (Windows / Linux).}
	Ce n\oe{}ud h\'eberge deux composants distincts~:
	
	\begin{itemize}[itemsep=2pt]
		\item \textbf{Frontend Server (:5173 dev / :80 prod)~:} sert le build statique
		Vite (\texttt{dist/}) en production.
		\item \textbf{Backend API Server --- Uvicorn (:8000)~:} ex\'ecute l'application
		FastAPI (Python~3.11, SQLAlchemy Async, Pydantic~v2, slowapi).
		Les analyses longues sont d\'el\'egu\'ees \`a \texttt{asyncio.BackgroundTask}
		pour ne pas bloquer la boucle d'\'ev\'enements.
		Le composant \textbf{Alembic Migrations SQL} est invoqu\'e au d\'emarrage
		pour maintenir le sch\'ema PostgreSQL \`a jour.
		La \textbf{Couche IA} embarqu\'ee regroupe le parser de documents,
		le moteur RAG, le client Mistral et les Sentence Transformers
		(mod\`ele \texttt{MiniLM-L12-v2}).
	\end{itemize}
	
	\paragraph{MinIO Server (:9000 / :9001).}
	Moteur de stockage objet compatible S3.
	Le bucket \texttt{grc-reports} re\c{c}oit les fichiers originaux
	(PDF, DOCX, XLSX) via le \textbf{HTTP SDK MinIO}.
	Le port~9001 expose la console d'administration.
	
	\paragraph{PostgreSQL Server (:5433).}
	Base de donn\'ees relationnelle h\'ebergeant la base \texttt{grc\_db}
	avec six tables principales~: \texttt{utilisateurs}, \texttt{rapports},
	\texttt{analyses}, \texttt{risques}, \texttt{conformite} et \texttt{recommandations}.
	Les acc\`es runtime utilisent le driver \textbf{asyncpg/TCP}~;
	les migrations Alembic utilisent psycopg2 (synchrone).
	
	\paragraph{ChromaDB Server (:8001).}
	Base vectorielle ex\'ecut\'ee en mode HTTP (\texttt{chroma run}).
	Le serveur Chroma HTTP re\c{c}oit les requ\^etes d'indexation
	(chunks + embeddings) et de recherche s\'emantique (top-$k$ cosinus)
	depuis la couche IA via \textbf{HTTP}.
	Les vecteurs et chunks sont persist\'es dans \texttt{./chroma\_data}.
	
	\paragraph{Mistral AI Cloud (\texttt{api.mistral.ai}) --- acteur externe.}
	Seul composant ext\'erieur au p\'erim\`etre de d\'eploiement local.
	Le mod\`ele \texttt{mistral-large-latest} est invoqu\'e via \textbf{HTTPS + API Key}
	pour cinq t\^aches~: analyse des risques, \'evaluation de conformit\'e,
	recommandations, r\'esum\'e ex\'ecutif et chat RAG.
	Aucune donn\'ee sensible n'est stock\'ee c\^ot\'e Mistral~;
	seuls des extraits de texte anonymis\'es transitent dans les prompts.
	
	\paragraph{Protocoles de communication.}
	Le tableau de l\'egende du diagramme synth\'etise les protocoles utilis\'es~:
	HTTP/REST + JWT (client $\leftrightarrow$ API), asyncpg/TCP (API $\leftrightarrow$ PostgreSQL),
	HTTP SDK (API $\leftrightarrow$ MinIO), HTTP (API $\leftrightarrow$ ChromaDB),
	et HTTPS + API Key (API $\leftrightarrow$ Mistral AI).
	
	% ============================================================
	\section{Synth\`ese du chapitre}
	% ============================================================
	
	Ce chapitre a \'etabli les fondements conceptuels de GRC AI Analyzer. Les 24 besoins fonctionnels et 13 besoins non fonctionnels ont \'et\'e identifi\'es et organis\'es en modules coh\'erents. Les diagrammes UML --- cas d'utilisation, classes, s\'equence, activit\'e, architecture globale et d\'eploiement --- constituent la r\'ef\'erence compl\`ete de toute la phase d'impl\'ementation. Le chapitre suivant d\'etaille les choix techniques et l'architecture retenue.
	
	\chapter{Conception technique et choix technologiques}
	
	\begin{tcolorbox}[infobox]
		\itshape Ce chapitre suit le fil de la donn\'ee~: depuis l'upload d'un
		document jusqu'\`a la r\'eponse du chat RAG. Pour chaque \'etape,
		les choix technologiques sont justifi\'es, les d\'ecisions de conception
		expos\'ees, et les extraits de code repr\'esentatifs comment\'es.
	\end{tcolorbox}
	
	% =============================================================================
	\section{Pile technologique retenue}
	% =============================================================================
	
	\definecolor{fecolor}{RGB}{40,60,110}
	\definecolor{becolor}{RGB}{30,50,90}
	\definecolor{aicolor}{RGB}{30,90,50}
	\definecolor{dbcolor}{RGB}{80,80,80}
	\definecolor{seccolor}{RGB}{150,30,30}
	
	GRC AI Analyzer repose sur cinq couches technologiques s\'electionn\'ees
	pour satisfaire trois contraintes~: \textbf{performance} (pipeline IA
	parall\`ele), \textbf{robustesse} (persistance multi-moteurs) et
	\textbf{s\'ecurit\'e} (JWT, bcrypt, validation stricte, rate limiting).
	L'ensemble de la pile s'appuie sur des briques open-source \'eprouv\'ees~:
	React~18~\cite{react18} et Vite~\cite{vite} c\^ot\'e client, FastAPI~\cite{fastapi},
	SQLAlchemy~\cite{sqlalchemy}, Alembic~\cite{alembic} et Pydantic~\cite{pydantic}
	c\^ot\'e serveur, PostgreSQL~15~\cite{postgresql} et MinIO~\cite{minio} pour la
	persistance, ChromaDB~\cite{chromadb} et LangChain~\cite{langchain_rag} pour le
	pipeline RAG, et Zustand~\cite{zustand} pour la gestion d'\'etat.
	
	\begin{table}[H]
		\centering
		\begin{tabularx}{\textwidth}{@{}>{\bfseries\color{titleblue}}l l X@{}}
			\toprule
			\headrow \colhead{Couche} & \colhead{Technologie} & \colhead{R\^ole / justification} \\
			\midrule
			Interface & React 18 + Vite + TypeScript & SPA avec HMR, typage statique, code-splitting \\
			\rowcolor{rowalt} Interface & Zustand + Axios & \'Etat global JWT, intercepteur token auto-inject\'e \\
			Interface & Recharts & Matrice risques (ScatterChart) et radar conformit\'e \\
			\midrule
			Backend & FastAPI 0.115 + Python 3.11 & ASGI async, doc OpenAPI auto, BackgroundTask \\
			\rowcolor{rowalt} Backend & SQLAlchemy 2 + asyncpg & ORM async natif, z\'ero thread bloquant \\
			Backend & Alembic + psycopg2 & Migrations versionn\'ees (driver sync pour Alembic) \\
			\bottomrule
		\end{tabularx}

	\end{table}
	
		\begin{table}[H]
		\centering
		\begin{tabularx}{\textwidth}{@{}>{\bfseries\color{titleblue}}l l X@{}}
			\toprule
			\rowcolor{rowalt} Backend & Pydantic v2 & Validation entr\'ees, enveloppe \texttt{\{data,message,success\}} \\
			\midrule
			IA / NLP & Mistral Large (API cloud) & LLM central~: extraction, conformit\'e, chat RAG \\
			\rowcolor{rowalt} IA / NLP & sentence-transformers MiniLM & Embeddings 384 dims, 50+ langues, singleton \\
			\midrule
			Infrastructure & PostgreSQL 15 & SGBDR principal, 6 entit\'es UUID, enums natifs \\
			\rowcolor{rowalt} Infrastructure & MinIO (S3-compatible) & Stockage objet des fichiers bruts \\
			Infrastructure & ChromaDB (HTTP :8001) & Base vectorielle HNSW cosinus, 1 collection/analyse \\
			\midrule
			S\'ecurit\'e & JWT HS256 + bcrypt & Tokens stateless, salt al\'eatoire par appel \\
			\rowcolor{rowalt} S\'ecurit\'e & slowapi + magic bytes & Rate limiting IP, validation signature binaire \\
			\bottomrule
		\end{tabularx}
		\caption{Pile technologique de GRC AI Analyzer}
		\label{tab:stack}
	\end{table}
	
	% =============================================================================
	\section{Architecture en couches du backend}
	% =============================================================================
	
	Le backend est organis\'e en cinq couches verticales strictement
	s\'epar\'ees~: une couche ne peut appeler que les couches
	sous-jacentes, ce qui garantit la testabilit\'e unitaire de chaque niveau.
	
	\begin{figure}[H]
		\centering
		\begin{tikzpicture}[node distance=0.28cm]
			\tikzstyle{layer}=[rectangle,draw=darknavy,thick,minimum width=14cm,
			minimum height=0.85cm,rounded corners=2pt,align=center,font=\small]
			\node[layer,fill=fecolor!20]  (api)
			{\textbf{Couche API} \enspace \texttt{app/api/v1/}
				\enspace --- routes FastAPI, validation Pydantic, aucune logique m\'etier};
			\node[layer,fill=becolor!15,below=of api] (svc)
			{\textbf{Couche Services} \enspace \texttt{app/services/}
				\enspace --- orchestration~: upload, analyse, export, chat};
			\node[layer,fill=aicolor!15,below=of svc] (ai)
			{\textbf{Couche IA} \enspace \texttt{app/ai/}
				\enspace --- prompts, parseurs, client Mistral, RAG, embeddings};
			\node[layer,fill=dbcolor!10,below=of ai] (model)
			{\textbf{Couche Mod\`eles} \enspace \texttt{app/models/}
				\enspace --- ORM SQLAlchemy 2, 6 entit\'es, enums PostgreSQL};
			\node[layer,fill=seccolor!10,below=of model] (core)
			{\textbf{Couche Core} \enspace \texttt{app/core/}
				\enspace --- JWT, d\'ependances FastAPI, rate limiter, Pydantic Settings};
		\end{tikzpicture}
		\caption{Organisation en cinq couches du backend FastAPI}
		\label{fig:layers}
	\end{figure}
	
	Le point d'entr\'ee \texttt{main.py} enregistre les cinq routeurs
	et configure les middlewares globaux~:
	
	\begin{lstlisting}[language=Python,
		caption={Configuration CORS, rate-limit et routeurs (main.py)}]
		app = FastAPI(title="GRC AI Analyzer", version="1.0.0")
		
		# Rate limiter global -- HTTP 429 si depassement par IP
		app.state.limiter = limiter
		app.add_exception_handler(RateLimitExceeded, _rate_limit_exceeded_handler)
		
		# CORS restreint aux origines connues (frontend Vite :5173 et prod)
		app.add_middleware(CORSMiddleware,
		allow_origins=[settings.frontend_url, "http://localhost:5173"],
		allow_credentials=True, allow_methods=["*"], allow_headers=["*"])
		
		# Cinq routeurs metier, tous sous le prefixe /api/v1
		app.include_router(auth.router,     prefix="/api/v1")  # /login, /register
		app.include_router(rapports.router, prefix="/api/v1")  # /rapports/*
		app.include_router(analyses.router, prefix="/api/v1")  # /analyses/*
		app.include_router(chat.router,     prefix="/api/v1")  # /chat/*
		app.include_router(export.router,   prefix="/api/v1")  # /export/*
	\end{lstlisting}
	
	La configuration globale est centralis\'ee via Pydantic Settings,
	ce qui garantit que le fichier \texttt{.env} est r\'esolu
	ind\'ependamment du r\'epertoire courant au d\'emarrage~:
	
	\begin{lstlisting}[language=Python,
		caption={Configuration via Pydantic Settings (app/config.py)}]
		class Settings(BaseSettings):
		database_url: str              # postgresql+asyncpg://...
		mistral_api_key: str
		mistral_model: str = "mistral-large-latest"
		secret_key: str                # secrets.token_hex(32)
		algorithm: str = "HS256"
		access_token_expire_minutes: int = 480
		minio_endpoint: str = "localhost:9000"
		minio_bucket: str  = "grc-reports"
		frontend_url: str  = "http://localhost:5173"
		chroma_host: str   = "localhost"
		chroma_port: int   = 8001
		
		model_config = SettingsConfigDict(
		# Chemin absolu : fonctionne quel que soit le cwd
		env_file=Path(__file__).parent.parent / ".env",
		extra="ignore"
		)
		
		settings = Settings()   # singleton importe dans toute l'application
	\end{lstlisting}
	
	% =============================================================================
	\section{Conception du mod\`ele de donn\'ees}
	% =============================================================================
	
	Le mod\`ele relationnel repose sur six entit\'es PostgreSQL
	reli\'ees par cl\'es \'etrang\`eres UUID~v4.
	
	\begin{figure}[H]
		\centering
		\begin{tikzpicture}[node distance=1cm and 1.8cm,>={Latex[length=2mm]}]
			\tikzstyle{ent}=[rectangle,draw=darknavy,thick,fill=lightgray,
			minimum width=3.2cm,minimum height=0.85cm,align=center,font=\small\bfseries]
			\node[ent] (user)    {Utilisateur};
			\node[ent,right=2.2cm of user]    (rapport)  {Rapport};
			\node[ent,right=2.2cm of rapport] (analyse)  {Analyse};
			\node[ent,below=1.6cm of analyse,xshift=-4cm] (risque) {Risque};
			\node[ent,below=1.6cm of analyse]              (conf)   {ResultatConformite};
			\node[ent,below=1.6cm of analyse,xshift=4cm]   (reco)   {Recommandation};
			\draw[->] (user)    -- node[above]{\tiny 1..n} (rapport);
			\draw[->] (rapport) -- node[above]{\tiny 1..1} (analyse);
			\draw[->] (analyse) -- (risque);
			\draw[->] (analyse) -- (conf);
			\draw[->] (analyse) -- (reco);
		\end{tikzpicture}
		\caption{Mod\`ele relationnel de GRC AI Analyzer (PostgreSQL)}
		\label{fig:erd}
	\end{figure}
	
	L'entit\'e \texttt{Analyse} est le pivot~: elle aggr\`ege les r\'esultats
	des trois axes GRC. La relation optionnelle \texttt{Risque} $\to$
	\texttt{Recommandation} (\texttt{risque\_id} FK nullable) permet
	\`a Mistral de lier une recommandation \`a un risque pr\'ecis
	tout en autorisant des recommandations globales non li\'ees.
	
	\begin{lstlisting}[language=Python,
		caption={Mod\`ele ORM \texttt{Risque} --- SQLAlchemy 2 (models/risque.py)}]
		class Risque(Base):
		__tablename__ = "risques"
		
		id:           Mapped[uuid.UUID] = mapped_column(
		UUID(as_uuid=True), primary_key=True,
		default=uuid.uuid4)
		analyse_id:   Mapped[uuid.UUID] = mapped_column(
		ForeignKey("analyses.id", ondelete="CASCADE"))
		libelle:      Mapped[str]  = mapped_column(String(500))
		categorie:    Mapped[str]  = mapped_column(
		Enum("CYBER","OPERATIONNEL","LEGAL","FINANCIER","RH",
		name="categorie_risque_enum"))
		probabilite:  Mapped[int]   = mapped_column(Integer)   # 1--5
		impact:       Mapped[int]   = mapped_column(Integer)   # 1--5
		score_risque: Mapped[float] = mapped_column(Float)     # prob x impact
		severite:     Mapped[str]   = mapped_column(
		Enum("CRITIQUE","ELEVE","MOYEN","FAIBLE",
		name="severite_enum"))
		section_source: Mapped[Optional[str]] = mapped_column(String(500))
	\end{lstlisting}
	
	La clause \texttt{ondelete="CASCADE"} garantit la suppression
	automatique en cascade~: supprimer une analyse entra\^ine la
	suppression de tous ses risques, r\'esultats et recommandations
	sans logique applicative suppl\'ementaire.
	La formule de s\'ev\'erit\'e est d\'eterministe~:
	
	\[
	\text{score} = p \times i,\quad p,i \in \{1,\ldots,5\}
	\qquad
	\text{s\'ev\'erit\'e} =
	\begin{cases}
		\textsc{Faible}   & \text{score} < 6 \\
		\textsc{Moyen}    & 6 \leq \text{score} < 12 \\
		\textsc{\'Elev\'e}    & 12 \leq \text{score} < 20 \\
		\textsc{Critique} & \text{score} \geq 20
	\end{cases}
	\]
	
	% =============================================================================
	
	\subsection{Architecture des bases de donn\'ees}
	
	GRC AI Analyzer utilise trois moteurs de stockage compl\'ementaires,
	chacun optimis\'e pour un type de donn\'ees diff\'erent~:
	\textbf{PostgreSQL} pour les donn\'ees relationnelles structur\'ees,
	\textbf{MinIO} pour les fichiers binaires bruts,
	et \textbf{ChromaDB} pour les vecteurs s\'emantiques du moteur RAG.
	
	\subsubsection*{Sch\'ema PostgreSQL}
	
	PostgreSQL stocke les \textbf{six entit\'es m\'etier persist\'ees}. Les trois classes conceptuelles non persist\'ees (\texttt{MessageChat}, \texttt{ReferentielGRC}, \texttt{PisteAudit}) sont des abstractions UML sans table physique.
	Le diagramme ci-dessous montre le sch\'ema physique g\'en\'er\'e
	par Alembic~:
	
	\begin{figure}[H]
		\centering
		\includegraphics[width=0.8\textwidth]{figures/Postgre.png}
		\caption{Sch\'ema physique PostgreSQL --- tables et cl\'es \'etrang\`eres}
		\label{fig:db-postgres}
	\end{figure}
	
	Les conventions de nommage sont~: cl\'es primaires \texttt{UUID~v4},
	cl\'es \'etrang\`eres avec \texttt{ON DELETE CASCADE},
	enums PostgreSQL natifs (\texttt{categorie\_risque\_enum},
	\texttt{severite\_enum}, \texttt{statut\_analyse\_enum}).
	
	\subsubsection*{Stockage objet MinIO}
	
	MinIO stocke les fichiers bruts uploadés dans le bucket \texttt{grc-reports}
	sous la structure \texttt{reports/\{uuid\}/\{nom\_sanitisé\}}~:
	
	\begin{figure}[H]
		\centering
		\includegraphics[width=0.80\textwidth]{figures/minio.png}
		\caption{Console MinIO --- bucket \texttt{grc-reports} et structure des objets}
		\label{fig:db-minio}
	\end{figure}
	
	\subsubsection*{Collections vectorielles ChromaDB}
	
	ChromaDB maintient une collection par analyse, nommée
	\texttt{analyse\_\{uuid\}} (tirets remplacés par underscores).
	Chaque collection contient les chunks avec leurs embeddings
	384-dimensionnels et leurs métadonnées~:
	
	\begin{figure}[H]
		\centering
		\includegraphics[width=0.8\textwidth]{figures/chromadb.png}
		\caption{Collections ChromaDB --- une collection par analyse, index HNSW cosinus}
		\label{fig:db-chroma}
	\end{figure}
	
	\section{Conception des routes API}
	% =============================================================================
	
	Toutes les routes suivent la convention \texttt{/api/v1/\{ressource\}}
	et retournent une enveloppe JSON uniforme
	\texttt{\{"data":~...,~"message":~"...",~"success":~bool\}}.
	Le tableau~\ref{tab:routes} r\'ecapitule les endpoints principaux.
	
	\begin{table}[H]
		\centering
		\begin{tabularx}{\textwidth}{@{}>{\ttfamily\bfseries}l >{\ttfamily}l X@{}}
			\toprule
			\headrow \colhead{M\'ethode} & \colhead{\rmfamily Route} & \colhead{\rmfamily Description} \\
			\midrule
			POST & /auth/login & Authentification~; retourne un token JWT HS256 \\
			\rowcolor{rowalt} POST & /auth/register & Cr\'eation de compte utilisateur \\
			POST & /rapports/upload & Upload PDF/DOCX/XLSX vers MinIO \\
			\rowcolor{rowalt} GET  & /rapports/ & Liste des rapports de l'utilisateur \\
			DELETE & /rapports/\{id\} & Supprime rapport + analyse + collection ChromaDB \\
			\rowcolor{rowalt} POST & /analyses/lancer/\{rapport\_id\} & D\'eclenche l'analyse en arri\`ere-plan (HTTP~202) \\
			GET  & /analyses/\{id\} & Statut et r\'esum\'e ex\'ecutif (\texttt{en\_cours} / \texttt{termine}) \\
			\rowcolor{rowalt} GET  & /analyses/\{id\}/risks & Liste des risques extraits \\
			GET  & /analyses/\{id\}/conformite & R\'esultats de conformit\'e par r\'ef\'erentiel \\
			\rowcolor{rowalt} GET  & /analyses/\{id\}/recommandations & Recommandations prioris\'ees \\
			POST & /chat/\{analyse\_id\} & Question RAG sur le rapport analys\'e \\
			\rowcolor{rowalt} GET  & /export/\{id\}/pdf & Export PDF du rapport complet \\
			GET  & /export/\{id\}/excel & Export Excel (audit trail) \\
			\bottomrule
		\end{tabularx}
		\caption{Routes API principales de GRC AI Analyzer}
		\label{tab:routes}
	\end{table}
	
	\subsection{Lancement d'une analyse (HTTP 202 + BackgroundTask)}
	
	La route la plus importante est \texttt{POST /analyses/lancer/\{rapport\_id\}}~:
	elle retourne imm\'ediatement un HTTP~202 pendant que l'analyse
	tourne en arri\`ere-plan dans une session de base de donn\'ees ind\'ependante.
	
	\begin{lstlisting}[language=Python,
		caption={Route de lancement d'analyse avec BackgroundTask (analyses.py)}]
		@router.post("/lancer/{rapport_id}", status_code=202)
		async def lancer_analyse(
		rapport_id: uuid.UUID,
		background_tasks: BackgroundTasks,
		db: Annotated[AsyncSession, Depends(get_db)],
		current_user: Annotated[Utilisateur, Depends(get_current_user)],
		):
		# Verification existence du rapport
		result = await db.execute(
		select(Rapport).where(Rapport.id == rapport_id)
		)
		rapport = result.scalar_one_or_none()
		if not rapport:
		raise HTTPException(status_code=404, detail="Rapport not found")
		
		# Creation de l'enregistrement analyse avec statut en_cours
		analyse = Analyse(
		id=uuid.uuid4(),
		rapport_id=rapport_id,
		lance_par=current_user.id,
		statut="en_cours",
		)
		db.add(analyse)
		rapport.statut = "en_cours"
		await db.commit()   # commit avant le background task
		
		# Lancement en arriere-plan (nouvelle session DB independante)
		background_tasks.add_task(
		_run_analysis_background, analyse.id, rapport_id
		)
		# Reponse immediate : le frontend va poller GET /analyses/{id}
		return api_response(
		data={"analyse_id": str(analyse.id), "statut": "en_cours"},
		message="Analysis started",
		)
	\end{lstlisting}
	
	Le commit \textit{avant} l'ajout de la t\^ache de fond est essentiel~:
	le background task ouvre sa propre session \texttt{AsyncSessionLocal}
	et doit trouver l'enregistrement en base. Sans ce commit,
	la t\^ache de fond \'echouerait avec une erreur de cl\'e \'etrang\`ere.
	
	\subsection{Polling du statut d'analyse (frontend)}
	
	Le frontend React poll GET \texttt{/analyses/\{id\}} toutes les
	3~secondes jusqu'\`a \texttt{statut === "termine"}~:
	
	\begin{lstlisting}[language=Python,
		caption={Endpoint de polling du statut d'analyse (analyses.py)}]
		@router.get("/{analyse_id}")
		async def get_one(
		analyse_id: uuid.UUID,
		db: Annotated[AsyncSession, Depends(get_db)],
		current_user: Annotated[Utilisateur, Depends(get_current_user)],
		):
		analyse = await get_analyse(db, analyse_id)
		if not analyse:
		raise HTTPException(status_code=404, detail="Analyse not found")
		return api_response(data={
			"id":              str(analyse.id),
			"rapport_id":      str(analyse.rapport_id),
			"resume_executif": analyse.resume_executif,
			"score_maturite":  analyse.score_maturite,
			"statut":          analyse.statut,   # "en_cours"|"termine"|"erreur"
			"created_at":      analyse.created_at.isoformat(),
		})
	\end{lstlisting}
	
	\subsection{Interface de chat RAG}
	
	L'intercepteur Axios injecte automatiquement le token JWT
	dans toutes les requ\^etes sortantes~:
	
	\begin{lstlisting}[language=Java,
		caption={Intercepteur JWT Axios (frontend/src/services/api.ts)}]
		const api = axios.create({ baseURL: import.meta.env.VITE_API_URL });
		
		// Injection automatique du token a chaque requete
		api.interceptors.request.use((config) => {
			const token = useAuthStore.getState().token;
			if (token) config.headers.Authorization = `Bearer ${token}`;
			return config;
		});
		
		// Deconnexion automatique si le token expire (401)
		api.interceptors.response.use(
		(res) => res,
		(err) => {
			if (err.response?.status === 401) {
				useAuthStore.getState().logout();
				window.location.href = "/login";
			}
			return Promise.reject(err);
		}
		);
	\end{lstlisting}
	
	Ce m\'ecanisme centralise la gestion du cycle de vie de
	l'authentification~: aucun composant React n'a \`a g\'erer
	explicitement le token.
	
	% =============================================================================
	\section{Conception des prompts IA}
	% =============================================================================
	
	GRC AI Analyzer effectue \textbf{six appels LLM} \`a Mistral~Large, r\'epartis en \textbf{quatre prompts sp\'ecialis\'es}~: le prompt d'extraction des risques (1 appel), le prompt de conformit\'e instanci\'e trois fois~--- ISO~27001:2022, RGPD, Loi~09-08 (3~appels), le prompt de recommandations (1 appel) et le prompt de r\'esum\'e ex\'ecutif (1 appel). Chaque appel comprend un \textit{system prompt} (r\^ole et r\`egles) et un \textit{user prompt} (donn\'ees + format JSON attendu).
	
	\begin{figure}[H]
		\centering
		\begin{tikzpicture}[node distance=0.5cm and 1.2cm]
			\tikzstyle{pm}=[rectangle,draw=darknavy,thick,fill=darknavy!12,
			minimum width=3cm,minimum height=0.8cm,rounded corners=3pt,
			align=center,font=\small]
			\tikzstyle{outbox}=[rectangle,draw=darkgreen,thick,fill=darkgreen!10,
			minimum width=3cm,minimum height=0.7cm,rounded corners=3pt,
			align=center,font=\small]
			\tikzstyle{arr}=[-{Latex[length=2mm]},thick]
			
			\node[pm] (p1) {Prompt\\extraction risques};
			\node[pm,right=1.4cm of p1] (p2) {Prompts\\conformit\'e (x3)};
			\node[pm,right=1.4cm of p2] (p3) {Prompt\\recommandations};
			\node[pm,right=1.4cm of p3] (p4) {Prompt\\r\'esum\'e ex\'ecutif};
			
			\node[outbox,below=0.8cm of p1] (o1) {JSON risques[]};
			\node[outbox,below=0.8cm of p2,align=center] (o2) {JSON domaines[]\\(x3 r\'ef\'erentiels)};
			\node[outbox,below=0.8cm of p3,align=center] (o3) {JSON recomman-\\dations[]};
			\node[outbox,below=0.8cm of p4] (o4) {Texte brut\\350--450 mots};
			
			\draw[arr] (p1)--(o1); \draw[arr] (p2)--(o2);
			\draw[arr] (p3)--(o3); \draw[arr] (p4)--(o4);
		\end{tikzpicture}
		\caption{Les six appels LLM Mistral (quatre prompts) et leurs sorties}
		\label{fig:prompts}
	\end{figure}
	
	\subsection{Prompt d'extraction des risques}
	
	Le prompt d'extraction est le plus complexe~: il d\'etecte
	automatiquement le type de document (scan de vuln\'erabilit\'es,
	rapport de pentest, audit, registre des risques, incident...) et
	adapte sa strat\'egie d'extraction en cons\'equence.
	
	\begin{lstlisting}[language=Python,
		caption={System prompt d'extraction des risques (risk\_prompts.py)}]
		RISK_SYSTEM_PROMPT = """You are a senior GRC and cybersecurity expert.
		Your task is to perform an EXHAUSTIVE extraction of every risk,
		vulnerability, finding, gap, weakness, and issue in the document.
		
		STEP 1 -- IDENTIFY THE DOCUMENT TYPE and apply the matching strategy:
		
		TYPE A -- VULNERABILITY SCAN (ZAP, Nessus, Burp, Qualys)
		- Extract EVERY individual alert -- one risk per alert.
		- DO NOT merge similar alerts (four CSP issues = four risks).
		- Severity mapping: Critical->P5/I5, High->P4/I4, Medium->P3/I3,
		Low->P2/I2, Informational->P1/I2.
		
		TYPE B -- PENETRATION TEST REPORT
		- Extract each finding with CVSS score and proof-of-concept.
		- CVSS: Critical(9+)->P5/I5, High(7-8.9)->P4/I4, Medium->P3/I3.
		
		TYPE C -- AUDIT / GAP ANALYSIS
		- Major non-conformance->P4/I4, Minor->P3/I3, Observation->P2/I2.
		- Specify the exact control/clause not met.
		
		TYPE D -- RISK REGISTER
		- Preserve original probability/impact scores when provided.
		
		TYPE G -- DPIA / PRIVACY REPORT
		- Reference specific GDPR/Loi 09-08 article for each risk.
		
		UNIVERSAL RULES:
		1. EXHAUSTIVE : nothing is too minor to extract.
		2. NO MERGING : each distinct finding = one risk object.
		3. GROUNDED   : every risk must be supported by document text.
		4. Respond ONLY in valid JSON. No text outside the JSON."""
	\end{lstlisting}
	
	\begin{lstlisting}[language=Python,
		caption={User prompt d'extraction des risques (risk\_prompts.py)}]
		RISK_USER_PROMPT = """Perform an exhaustive risk extraction.
		
		Return ONLY this JSON (no text outside):
		{
			"document_type": "VULNERABILITY_SCAN|PENTEST|AUDIT|RISK_REGISTER|...",
			"risques": [
			{
				"libelle":       "Exact name of the finding (max 200 chars)",
				"description":   "Full technical detail: CWE/CVE IDs, affected URLs,
				instance count, scanner evidence, remediation.
				Minimum 100 chars, maximum 1000 chars.",
				"categorie":     "CYBER|OPERATIONNEL|LEGAL|FINANCIER|RH",
				"probabilite":   3,
				"impact":        3,
				"section_source":"Alert ID, section, page, or finding ID",
				"confiance":     90
			}
			]
		}
		
		SCORING:
		- probabilite : likelihood of exploitation (1=rare, 5=near-certain)
		- impact      : business/security impact if it occurs (1=negligible, 5=catastrophic)
		- confiance   : how clearly stated in document (0-100; use >=60 for explicit)
		
		DOCUMENT TEXT:
		{text}"""
	\end{lstlisting}
	
	Le champ \texttt{confiance} (0--100) est un m\'eta-indicateur~:
	il permet au service m\'etier de filtrer les risques peu fiables
	(confiance~$<$~40) avant de les persister, \'evitant d'enregistrer
	des extrapolations hasardeuses de Mistral.
	
	\subsection{Prompts de v\'erification de conformit\'e}
	
	Le m\^eme template est utilis\'e trois fois, param\'etr\'e par le
	r\'ef\'erentiel (\texttt{ISO27001}, \texttt{RGPD}, \texttt{LOI0908}).
	Le system prompt instaure une philosophie d'\'evaluation en deux formes
	d'\'evidence~:
	
	\begin{lstlisting}[language=Python,
		caption={System prompt de conformit\'e --- philosophie d'\'evaluation (compliance\_prompts.py)}]
		COMPLIANCE_SYSTEM_PROMPT = """You are a {framework} compliance expert.
		
		EVALUATION PHILOSOPHY -- evaluate BOTH forms of evidence:
		
		1. EXPLICIT evidence: policies mentioned, controls described,
		the document directly addresses the requirement.
		
		2. IMPLICIT evidence: findings IMPLY the compliance state even
		without explicit mention. Examples:
		- Vulnerability scan showing missing HSTS -> A.13 weak
		- Audit finding CSRF vulnerabilities     -> access control gaps
		- Incident report describing a breach    -> A.16 incident mgmt risk
		
		SCORING GUIDE:
		- CONFORME     (75-100%) : clear evidence of implementation.
		- PARTIEL      (10-74%)  : partial or implicit evidence exists.
		- NON_CONFORME (0-9%)    : no evidence, or explicit failure.
		
		taux_global = weighted average of all domain scores.
		Respond ONLY in valid JSON. No text outside the JSON."""
	\end{lstlisting}
	
	\begin{lstlisting}[language=Python,
		caption={User prompt de conformit\'e ISO 27001 (extrait) (compliance\_prompts.py)}]
		COMPLIANCE_USER_PROMPT = """Evaluate compliance with {framework_display}.
		
		{domains}
		# Exemple pour ISO 27001 :
		# A.5  Information Security Policies
		# A.9  Access Control
		# A.12 Operational Security (vulnerability mgmt, logging, patching)
		# A.16 Information Security Incident Management
		# ... (14 domaines au total)
		
		Return ONLY this JSON:
		{
			"referentiel": "{framework}",
			"taux_global": 0,
			"domaines": [
			{
				"nom":      "Domain name",
				"statut":   "CONFORME|NON_CONFORME|PARTIEL",
				"taux":     0,
				"ecart":    "Specific gap description referencing document content.",
				"evidence": "Direct quote or reference from the document."
			}
			]
		}
		
		IMPORTANT: list ALL domains. taux_global = rounded average of taux[].
		
		DOCUMENT TEXT:
		{text}"""
	\end{lstlisting}
	
	La distinction \texttt{ecart} / \texttt{evidence} est d\'elib\'er\'ee~:
	\texttt{ecart} d\'ecrit le probl\`eme (ce qui manque), \texttt{evidence}
	cite le document (ce qui justifie le score). Cette s\'eparation permet
	au frontend d'afficher une explication actionnable et une r\'ef\'erence
	source distinctes dans le tableau de conformit\'e.
	
	\subsection{Prompt de g\'en\'eration des recommandations}
	
	Le prompt de recommandations re\c{c}oit la liste des risques extraits
	en phase~1 et exige une recommandation \textit{technique et sp\'ecifique}
	par risque~:
	
	\begin{lstlisting}[language=Python,
		caption={System prompt des recommandations --- standards qualit\'e (recommendation\_prompts.py)}]
		RECOMMENDATION_SYSTEM_PROMPT = """You are a senior GRC consultant.
		Generate specific, actionable, technically detailed recommendations.
		
		Each recommendation MUST:
		1. DIRECTLY address the specific risk -- no generic advice.
		2. Include concrete steps (HTTP header values, config snippets,
		OWASP/NIST controls, specific tools).
		3. Reference industry standards (OWASP Top 10, NIST SP 800-53,
		ISO 27001 controls, GDPR articles, CWE mitigations).
		4. Distinguish QUICK_WIN (<2 weeks) from LONG_TERME.
		
		QUICK_WIN examples:
		- Add HTTP security headers (HSTS, X-Frame-Options, nosniff)
		- Disable X-Powered-By header in server config
		- Patch a vulnerable library version
		
		LONG_TERME examples:
		- Implement full CSP policy (design + allowlists)
		- Establish DPO role and ROPA
		- Build incident response plan
		
		PRIORITIZATION: CRITIQUE risk -> CRITIQUE priority,
		ELEVE -> HAUTE, MOYEN -> MOYENNE, FAIBLE -> FAIBLE.
		
		Respond ONLY in valid JSON. No text outside the JSON."""
	\end{lstlisting}
	
	\begin{lstlisting}[language=Python,
		caption={User prompt des recommandations (recommendation\_prompts.py)}]
		RECOMMENDATION_USER_PROMPT = """Generate one recommendation per risk.
		
		IDENTIFIED RISKS:
		{risks_summary}
		
		Return ONLY this JSON:
		{
			"recommandations": [
			{
				"libelle":      "Concise action title (max 150 chars)",
				"description":  "Detailed steps: what to do, how to do it,
				specific values/commands/configs, expected
				outcome, standard reference. Min 150 chars.",
				"priorite":     "CRITIQUE|HAUTE|MOYENNE|FAIBLE",
				"type_action":  "QUICK_WIN|LONG_TERME",
				"effort_estime":"FAIBLE|MOYEN|ELEVE",
				"risque_lie":   "Exact libelle of the linked risk (verbatim)"
			}
			]
		}"""
	\end{lstlisting}
	
	Le champ \texttt{risque\_lie} copie verbatim le \texttt{libelle}
	du risque concern\'e, ce qui permet au service m\'etier de
	r\'e\'etablir la jointure \texttt{Recommandation.risque\_id}
	par recherche exacte dans la liste des risques persist\'es.
	
	\subsection{Prompt du r\'esum\'e ex\'ecutif}
	
	Le r\'esum\'e ex\'ecutif est le seul prompt qui retourne du texte brut
	(pas de JSON). Le system prompt impose un format strict
	pour \'eviter l'injection de Markdown dans le PDF export\'e~:
	
	\begin{lstlisting}[language=Python,
		caption={System prompt du r\'esum\'e ex\'ecutif (summary\_prompts.py)}]
		SUMMARY_SYSTEM_PROMPT = """You are a GRC consultant writing for a CISO.
		
		CRITICAL FORMAT RULES:
		1. Write ONLY plain text. NO Markdown whatsoever.
		- No #, ##, ### headings.
		- No **, __ bold or * italic.
		- No bullet points (*, -, +).
		- Use numbered sections: "1. KEY FINDINGS", "2. TOP RISKS", etc.
		2. Write in French. Professional, executive-level.
		3. Be factual -- only reference data provided.
		4. Length: 350-450 words total.
		
		STRUCTURE:
		1. CONTEXTE ET OBJET DU DOCUMENT (2-3 sentences)
		2. PRINCIPALES CONCLUSIONS (numbered list, 4-6 items)
		3. RISQUES PRIORITAIRES (top 3-5 with severity + impact statement)
		4. ETAT DE CONFORMITE  (ISO: X% | RGPD: X% | Loi 09-08: X%)
		5. ACTIONS IMMEDIATES RECOMMANDEES (top 3, numbered)
		6. CONCLUSION (1 paragraph)"""
	\end{lstlisting}
	
	\begin{lstlisting}[language=Python,
		caption={User prompt du r\'esum\'e ex\'ecutif avec donn\'ees agr\'eg\'ees}]
		SUMMARY_USER_PROMPT = """Write the executive summary using the data below.
		
		ANALYSIS DATA:
		- Total risks identified    : {risks_count}
		- Risks by severity         : {risks_by_severity}
		- Top risks (most critical) : {top_risks}
		- ISO 27001 compliance      : {iso_score}%
		- RGPD compliance           : {rgpd_score}%
		- Loi 09-08 compliance      : {loi_score}%
		- Overall maturity score    : {maturity_score}%
		- Recommendations generated : {recs_count}
		
		DOCUMENT EXCERPT (first 4000 chars):
		{text_excerpt}"""
	\end{lstlisting}
	
	Les chiffres inject\'es dans le user prompt (\texttt{risks\_count},
	\texttt{iso\_score}...) proviennent des r\'esultats de la phase~1~:
	le r\'esum\'e est donc ancr\'e sur des valeurs num\'eriques r\'eelles
	et non sur la perception de Mistral, ce qui \'elimine les
	hallucinations chiffr\'ees.
	
	% =============================================================================
	\section{Pipeline d'analyse IA asynchrone}
	% =============================================================================
	
	Le c\oe{}ur technique de la plateforme est le pipeline asynchrone
	en deux phases \texttt{asyncio.gather} successives.
	La phase~1 lance cinq t\^aches simultan\'ement~; la phase~2
	consomme leurs r\'esultats agr\'eg\'es.
	
	\begin{figure}[H]
		\centering
		\begin{tikzpicture}[node distance=0.65cm and 0.55cm]
			\tikzstyle{se}=[rectangle,draw=darknavy,thick,fill=darknavy!15,
			minimum width=3cm,minimum height=0.75cm,rounded corners=3pt,
			align=center,font=\small]
			\tikzstyle{ai}=[rectangle,draw=darknavy,thick,fill=lightgray,
			minimum width=2.6cm,minimum height=0.75cm,rounded corners=2pt,
			align=center,font=\small]
			\tikzstyle{bar}=[rectangle,draw=darkgreen,thick,fill=darkgreen!12,
			minimum width=13cm,minimum height=0.75cm,rounded corners=2pt,
			align=center,font=\small\bfseries]
			
			\node[se] (start) {Upload + parsing\\+ chunking};
			
			\node[ai,below right=0.7cm and -5.8cm of start] (p1a) {Indexation\\ChromaDB};
			\node[ai,right=0.3cm of p1a] (p1b) {Extraction\\risques};
			\node[ai,right=0.3cm of p1b] (p1c) {ISO~27001};
			\node[ai,right=0.3cm of p1c] (p1d) {RGPD};
			\node[ai,right=0.3cm of p1d] (p1e) {Loi~09-08};
			
			\node[bar,below=1.1cm of p1c] (g1) {asyncio.gather() --- Phase 1 \quad (return\_exceptions=True)};
			
			\node[ai,below left=0.8cm and -2cm of g1] (p2a) {Recommandations};
			\node[ai,below right=0.8cm and -2cm of g1] (p2b) {R\'esum\'e ex\'ecutif};
			
			\node[bar,below=1.8cm of g1] (g2) {asyncio.gather() --- Phase 2};
			\node[se,below=0.6cm of g2] (endn) {statut = \texttt{termin\'e}};
			
			\foreach \n in {p1a,p1b,p1c,p1d,p1e}
			\draw[-{Latex[length=2mm]}] (start) -- (\n);
			\foreach \n in {p1a,p1b,p1c,p1d,p1e}
			\draw[-{Latex[length=2mm]}] (\n) -- (g1);
			\draw[-{Latex[length=2mm]}] (g1) -- (p2a);
			\draw[-{Latex[length=2mm]}] (g1) -- (p2b);
			\draw[-{Latex[length=2mm]}] (p2a) -- (g2);
			\draw[-{Latex[length=2mm]}] (p2b) -- (g2);
			\draw[-{Latex[length=2mm]}] (g2) -- (endn);
		\end{tikzpicture}
		\caption{Pipeline d'analyse IA en deux phases parall\`eles}
		\label{fig:pipeline}
	\end{figure}
	
	\subsection{Phase~1 --- cinq t\^aches simultan\'ees (4~appels LLM + 1~indexation)}
	
	\begin{lstlisting}[language=Python,
		caption={Phase 1 : indexation + extraction + 3 conformit\'es en parall\`ele (analysis\_service.py)}]
		# Construction des taches (pas encore lancees)
		task_chroma = index_document_chunks(str(analyse_id), chunks)
		
		risk_sys, risk_usr = build_risk_prompt(risk_text)
		task_risk = call_mistral_json(risk_sys, risk_usr)
		
		tasks_comp = []
		for framework in ["ISO27001", "RGPD", "LOI0908"]:
		sys_p, usr_p = build_compliance_prompt(comp_text, framework)
		tasks_comp.append(call_mistral_json(sys_p, usr_p))
		
		# Lancement simultane des 5 taches
		# return_exceptions=True : une erreur partielle ne stoppe pas l'analyse
		phase1 = await asyncio.gather(
		task_chroma, task_risk, *tasks_comp,
		return_exceptions=True
		)
		# phase1[0] = nb chunks indexes (ou Exception si ChromaDB down)
		# phase1[1] = dict risques Mistral
		# phase1[2..4] = dict conformite ISO, RGPD, Loi 09-08
	\end{lstlisting}
	
	\subsection{Phase 2 --- recommandations et r\'esum\'e}
	
	La phase~2 re\c{c}oit les r\'esultats agr\'eg\'es de la phase~1.
	Cette s\'eparation est impos\'ee par une d\'ependance de donn\'ees~:
	les recommandations n\'ecessitent de conna\^itre \textit{\`a la fois}
	les risques ET les taux de conformit\'e.
	
	\begin{lstlisting}[language=Python,
		caption={Phase 2 : recommandations + r\'esum\'e en parall\`ele (analysis\_service.py)}]
		# Consolidation des resultats de phase 1
		gap_analysis = _build_gap_analysis(conformity_results)
		risks_summary = _format_risks_for_prompt(risks_extracted)
		
		reco_sys, reco_usr = build_recommendation_prompt(
		risks_summary=risks_summary,
		text_excerpt=risk_text[:3000],
		)
		summary_sys, summary_usr = build_summary_prompt(
		text=risk_text,
		risks_count=len(risks_extracted),
		iso_score=conformity_results["ISO27001"]["taux_global"],
		rgpd_score=conformity_results["RGPD"]["taux_global"],
		loi_score=conformity_results["LOI0908"]["taux_global"],
		risks_by_severity=severity_counts,
		top_risks=top5_risks,
		recs_count=len(risks_extracted),
		)
		# Recommandations (JSON) et resume (texte brut) en parallele
		phase2 = await asyncio.gather(
		call_mistral_json(reco_sys, reco_usr),
		call_mistral(summary_sys, summary_usr),   # texte brut, pas JSON
		return_exceptions=True
		)
	\end{lstlisting}
	
	\subsection{S\'erialisation anti-429 des appels Mistral}
	
	Les cinq t\^aches de la phase~1 sont lanc\'ees simultan\'ement
	par \texttt{asyncio.gather}, mais les appels Mistral sont
	s\'erialis\'es par un \texttt{Semaphore(1)} global avec
	backoff exponentiel en cas de HTTP~429~:
	
	\begin{lstlisting}[language=Python,
		caption={Client Mistral --- s\'emaphore et backoff (mistral\_client.py)}]
		_semaphore         = asyncio.Semaphore(1)
		_rate_limited_until: float = 0.0
		_last_call_at:      float = 0.0
		_INTER_CALL_DELAY   = 1.5   # secondes minimum entre deux appels
		
		async def call_mistral(system_prompt, user_prompt,
		temperature=0.7, max_tokens=4096,
		max_retries=5) -> str:
		for attempt in range(1, max_retries + 1):
		try:
		async with _semaphore:
		# Attendre la fin d'un eventuel cooldown rate-limit
		cooldown = _rate_limited_until - time.monotonic()
		if cooldown > 0:
		await asyncio.sleep(cooldown)
		# Espacement minimum entre appels consecutifs
		gap = _last_call_at + _INTER_CALL_DELAY - time.monotonic()
		if gap > 0:
		await asyncio.sleep(gap)
		_last_call_at = time.monotonic()
		response = await client.chat.complete_async(
		model=settings.mistral_model,
		messages=[{"role":"system","content":system_prompt},
		{"role":"user",  "content":user_prompt}],
		temperature=temperature,
		max_tokens=max_tokens,
		)
		return response.choices[0].message.content
		except Exception as exc:
		if _is_rate_limit_error(exc):        # HTTP 429
		wait = 30 * attempt              # 30s, 60s, 90s...
		_rate_limited_until = time.monotonic() + wait
		await asyncio.sleep(wait)
	\end{lstlisting}
	
	Le s\'emaphore r\'esout une tension fondamentale~:
	\texttt{asyncio.gather} veut lancer tous les appels imm\'ediatement,
	mais l'API Mistral impose un d\'ebit limit\'e.
	Le s\'emaphore sert de file d'attente FIFO~: les cinq coroutines
	sont toutes ``actives'' du point de vue d'\texttt{asyncio},
	mais une seule d\'etient le verrou et appelle r\'eellement l'API.
	
	% =============================================================================
	\section{Parseur de documents et pipeline RAG}
	% =============================================================================
	
	\subsection{Extraction de texte multi-format}
	
	Le module \texttt{document\_parser.py} normalise les trois formats
	en texte brut structur\'e, pr\'eservant les marqueurs de sections
	pour que Mistral puisse citer la source pr\'ecise d'un risque~:
	
	\begin{lstlisting}[language=Python,
		caption={Extraction de texte PDF/DOCX/XLSX (document\_parser.py)}]
		def extract_text(file_bytes: bytes, file_format: str) -> str:
		fmt = file_format.lower().strip(".")
		if fmt == "pdf":
		doc = fitz.open(stream=file_bytes, filetype="pdf")
		return "\n\n".join(
		f"=== Page {i+1} ===\n{p.get_text()}"
		for i, p in enumerate(doc)
		)
		elif fmt == "docx":
		doc = Document(io.BytesIO(file_bytes))
		parts = [p.text for p in doc.paragraphs if p.text.strip()]
		for table in doc.tables:   # contenu des tableaux inclus
		for row in table.rows:
		parts.append(" | ".join(c.text for c in row.cells))
		return "\n".join(parts)
		elif fmt == "xlsx":
		wb = openpyxl.load_workbook(io.BytesIO(file_bytes), data_only=True)
		parts = []
		for sheet in wb.worksheets:
		parts.append(f"=== Feuille : {sheet.title} ===")
		for row in sheet.iter_rows(values_only=True):
		line = " | ".join(str(v) for v in row if v is not None)
		if line.strip():
		parts.append(line)
		return "\n".join(parts)
		
		def chunk_text(text: str,
		chunk_size: int = 1500,
		overlap: int = 200) -> list[str]:
		"""Fenetres glissantes : garantit le contexte aux frontieres."""
		chunks, start = [], 0
		while start < len(text):
		chunks.append(text[start : start + chunk_size])
		if start + chunk_size >= len(text):
		break
		start += chunk_size - overlap   # avance de 1300 chars
		return chunks
	\end{lstlisting}
	
	\subsection{Indexation et recherche vectorielle ChromaDB}
	
	\begin{lstlisting}[language=Python,
		caption={Indexation des chunks (vector\_store.py)}]
		async def index_document_chunks(analyse_id: str,
		chunks: list[str]) -> int:
		collection = await asyncio.to_thread(
		get_chroma_client().get_or_create_collection,
		name=f"analyse_{analyse_id.replace('-', '_')}",
		metadata={"hnsw:space": "cosine"},   # distance cosinus
		)
		embeddings = await asyncio.to_thread(   # CPU -> thread
		get_embedding_model().encode, chunks, show_progress_bar=False
		)
		await asyncio.to_thread(collection.add,
		documents=chunks, embeddings=embeddings.tolist(),
		ids=[str(uuid.uuid4()) for _ in chunks],
		metadatas=[{"chunk_index": i} for i in range(len(chunks))],
		)
		return len(chunks)
	\end{lstlisting}
	
	\begin{lstlisting}[language=Python,
		caption={G\'en\'eration augment\'ee --- chat RAG (chat\_engine.py)}]
		async def answer_question(analyse_id: str,
		question: str, top_k: int = 5) -> dict:
		# 1. Retrieval : top-5 chunks semantiquement proches
		results = await search_similar_chunks(analyse_id, question, top_k)
		
		# 2. Construction du contexte
		context = "\n\n".join(
		f"[Extrait {i+1}]: {r['text']}"
		for i, r in enumerate(results)
		)
		# 3. Generation (temperature=0.3 : reponses factuelles)
		reponse = await call_mistral(
		CHAT_SYSTEM_PROMPT,
		CHAT_USER_PROMPT.format(context_chunks=context,
		question=question),
		temperature=0.3,    # faible creativite = moins d'hallucinations
		)
		return {"reponse": reponse,
			"sources": [r["text"][:300] for r in results]}
	\end{lstlisting}
	
	La \texttt{temperature=0.3} (vs.\ 0.7 par d\'efaut) r\'eduit
	la cr\'eativit\'e du mod\`ele au profit de la pr\'ecision factuelle~:
	dans un contexte d'audit GRC, une r\'eponse invent\'ee peut
	avoir des cons\'equences r\'eglementaires.
		
	% =============================================================================
	\section{S\'ecurit\'e applicative}
	% =============================================================================
	
	La s\'ecurit\'e de GRC AI Analyzer repose sur trois m\'ecanismes
	compl\'ementaires~: l'authentification \textit{stateless} par JWT,
	la validation stricte des fichiers entrants, et la limitation du
	d\'ebit des requ\^etes. Ces trois couches forment ensemble une
	d\'efense en profondeur adapt\'ee au contexte d'un prototype acad\'emique
	exposant des donn\'ees sensibles.
	
	% ---
	\subsection{Authentification JWT et hachage bcrypt}
	
	L'authentification de GRC AI Analyzer est \textit{stateless}~:
	aucune session n'est conserv\'ee c\^ot\'e serveur. \`A la connexion,
	le mot de passe de l'utilisateur est v\'erifi\'e par comparaison
	avec son hash \textbf{bcrypt} stock\'e en base. Bcrypt g\'en\`ere
	un sel al\'eatoire \`a chaque appel~; deux utilisateurs avec le
	m\^eme mot de passe obtiennent des empreintes diff\'erentes, ce
	qui \'elimine les attaques par \textit{rainbow table}.
	Si la v\'erification r\'eussit, un \textbf{JWT} (JSON Web Token)
	sign\'e avec HS256 est \'emis~; il encode l'identit\'e de
	l'utilisateur et expire apr\`es 480~minutes. Le client stocke
	ce token dans le \texttt{localStorage} via Zustand et le joint
	\`a chaque requ\^ete dans l'en-t\^ete \texttt{Authorization: Bearer}.
	\textit{(Limite v1~: le \texttt{localStorage} est accessible
		au JavaScript de la page, donc vuln\'erable \`a une attaque XSS~;
		un cookie \texttt{httpOnly} aurait \'et\'e plus s\^ur mais
		n\'ecessite une gestion CSRF suppl\'ementaire~; ce compromis est
		document\'e comme axe d'am\'elioration.)}
	
	\begin{lstlisting}[language=Python,
		caption={Hachage bcrypt et token JWT (core/security.py)}]
		def hash_password(password: str) -> str:
		# Salt aleatoire par appel : deux memes mdp -> hashs differents
		return bcrypt.hashpw(
		password.encode("utf-8"), bcrypt.gensalt()
		).decode("utf-8")
		
		def create_access_token(data: dict) -> str:
		to_encode = data.copy()
		to_encode["exp"] = datetime.now(timezone.utc) + timedelta(
		minutes=settings.access_token_expire_minutes  # 480 min
		)
		return jwt.encode(to_encode, settings.secret_key, algorithm="HS256")
	\end{lstlisting}
	
	% ---
	\subsection{Validation des fichiers (magic bytes)}
	
	L'upload de fichiers est un vecteur d'attaque classique~: un
	acteur malveillant pourrait renommer un ex\'ecutable en \texttt{.pdf}
	pour le faire traiter par le pipeline. GRC AI Analyzer d\'efend
	ce point en combinant \textbf{deux filtres}. Premi\`erement, une
	v\'erification par \textit{magic bytes}~: les premiers octets du
	fichier re\c{c}u sont compar\'es aux signatures binaires connues
	(\texttt{\%PDF} pour les PDF, \texttt{PK\textbackslash x03\textbackslash x04}
	pour les ZIP/DOCX/XLSX). Un fichier dont la signature ne correspond
	pas est rejet\'e imm\'ediatement, ind\'ependamment de son extension.
	Deuxi\`emement, une \textbf{sanitisation du nom de fichier}~:
	la fonction \'elimine les s\'equences de type \texttt{../../}
	(traversal de r\'epertoire) et substitue tout caract\`ere non
	alphanum\'erique par un tiret bas, avant de passer le nom \`a
	MinIO. Ensemble, ces deux filtres garantissent que seuls des
	documents l\'egitimes et non path-injecting sont accept\'es
	par le syst\`eme.
	
	\begin{lstlisting}[language=Python,
		caption={Validation par signature binaire et sanitisation (report\_service.py)}]
		_MAGIC = {
			"pdf":  [b"%PDF"],
			"docx": [b"PK\x03\x04"],   # Office Open XML = ZIP
			"xlsx": [b"PK\x03\x04"],
		}
		
		def _check_magic_bytes(data: bytes, fmt: str) -> None:
		sigs = _MAGIC.get(fmt, [])
		if sigs and not any(data.startswith(s) for s in sigs):
		raise ValueError(f"Signature binaire invalide pour '{fmt}'.")
		
		def _sanitize_filename(filename: str) -> str:
		name = Path(filename).name          # neutralise ../../
		safe = re.sub(r"[^\w.\-]", "_", name)
		return ("_" + safe) if safe.startswith(".") else (safe or "fichier")
	\end{lstlisting}
	
	% ---
	\subsection{Limitation du d\'ebit (\textit{rate limiting}) par adresse IP}
	
	Deux points d'entr\'ee de l'API sont particuli\`erement expos\'es
	aux abus~: l'endpoint de connexion, cible naturelle des attaques
	par force brute, et l'endpoint d'upload, potentiellement utilis\'e
	pour saturer le pipeline IA ou le stockage MinIO. GRC AI Analyzer
	prot\`ege ces routes via la biblioth\`eque \texttt{slowapi}
	(port FastAPI de Flask-Limiter)~: le login est limit\'e \`a
	\textbf{10 requ\^etes par minute} par adresse IP, et l'upload
	\`a \textbf{20 uploads par heure}. En compl\'ement, la taille
	du fichier est contr\^ol\'ee c\^ot\'e serveur~: tout fichier
	d\'epassant \textbf{20~Mo} est rejet\'e avec un HTTP~413 avant
	m\^eme d'\^etre transmis \`a MinIO. Cette combinaison \textit{rate
		limiting} + \textit{file size cap} pr\'evient les attaques DoS
	par \'epuisement des ressources de traitement.
	
	\begin{lstlisting}[language=Python,
		caption={Rate limiting sur les endpoints sensibles (auth.py, rapports.py)}]
		limiter = Limiter(key_func=get_remote_address)
		
		@router.post("/login")
		@limiter.limit("10/minute")   # protection brute-force
		async def login(request: Request, body: LoginRequest, ...): ...
		
		@router.post("/upload")
		@limiter.limit("20/hour")     # protection DoS
		async def upload_rapport(request: Request, file: UploadFile, ...):
		data = await file.read()
		if len(data) > 20 * 1024 * 1024:
		raise HTTPException(413, "Fichier trop volumineux (max 20 Mo)")
	\end{lstlisting}
	
	% =============================================================================
	\section{Synth\`ese du chapitre}
	% =============================================================================
	
	Ce chapitre a pr\'esent\'e la conception technique int\'egrale
	de GRC AI Analyzer en suivant le parcours de la donn\'ee~:
	upload $\to$ parsing $\to$ pipeline IA $\to$ API REST $\to$ interface.
	
	Les d\'ecisions cl\'es sont~:
	la d\'ecomposition en deux phases \texttt{asyncio.gather} qui maximise
	le parall\'elisme tout en respectant les d\'ependances de donn\'ees~;
	le s\'emaphore Mistral qui r\'esout la tension entre parall\'elisme
	logique et contraintes de d\'ebit API~;
	les quatre prompts sp\'ecialis\'es qui dirigent Mistral vers des
	sorties JSON strictement typ\'ees et actionables~;
	et l'architecture en cinq couches qui s\'epare clairement les
	responsabilit\'es.
	Le chapitre suivant pr\'esente les r\'esultats obtenus sur des
	rapports r\'eels.
	
	
	\chapter{Implémentation et résultats}
	
	\section{Environnement de développement}
	
	\subsection{Configuration de l'environnement}
	
	Le projet a été développé et testé dans l'environnement suivant~:
	
	\begin{table}[H]
		\centering
		\begin{tabularx}{\textwidth}{@{}>{\bfseries\color{titleblue}}l X@{}}
			\toprule
			\headrow \colhead{Composant} & \colhead{Version / D\'etail} \\
			\midrule
			Syst\`eme d'exploitation & Windows 11 + WSL2 (Ubuntu 22.04) \\
			\rowcolor{rowalt} Python & 3.11 \\
			Node.js & 20 LTS \\
			\rowcolor{rowalt} PostgreSQL & 15 (natif Windows, port 5433) \\
			MinIO & RELEASE.2024-01 (binaire Windows) \\
			\rowcolor{rowalt} ChromaDB & 0.4.x (pip, mode HTTP) \\
			Mistral AI & mistral-large-latest (API cloud) \\
			\rowcolor{rowalt} IDE & VS Code + extensions Python/Volar \\
			\bottomrule
		\end{tabularx}
		\caption{Environnement de d\'eveloppement}
		\label{tab:env-dev}
	\end{table}
	
	\subsection{Lancement du projet}
	
	Un script \texttt{start.bat} automatise le démarrage de l'ensemble des services backend (MinIO, ChromaDB, migrations Alembic, FastAPI)~:
	
	\begin{lstlisting}[language=bash, caption={Script de démarrage start.bat}]
		@echo off
		REM Lancement MinIO
		start "" "C:\minio\minio.exe" server C:\minio\data --address :9000 --console-address :9001
		
		REM Lancement ChromaDB
		start "" chroma run --path ./chroma_data --host 0.0.0.0 --port 8001
		
		REM Migrations Alembic
		cd backend
		call venv\Scripts\activate
		alembic upgrade head
		
		REM Demarrage backend FastAPI
		uvicorn main:app --reload --port 8000
	\end{lstlisting}
	
	L'exécution du script affiche un récapitulatif des services lancés avec leurs ports respectifs, comme illustré ci-dessous~:
	
	\begin{figure}[H]
		\centering
		\includegraphics[width=0.85\textwidth]{figures/startup_backend.png}
		\caption{Sortie du script \texttt{start.bat} --- démarrage des services backend}
		\label{fig:sc-startup-backend}
	\end{figure}
	
	Le script vérifie séquentiellement la disponibilité de MinIO (port 9000, console 9001), ChromaDB (port 8001), applique les migrations Alembic sur la base PostgreSQL, puis démarre l'API FastAPI sur le port 8000. Le frontend React est ensuite lancé séparément via Vite~:
	
	\begin{figure}[H]
		\centering
		\includegraphics[width=0.85\textwidth]{figures/startup_frontend.png}
		\caption{D\'emarrage du serveur de d\'eveloppement Vite (frontend React/Vite)}
		\label{fig:sc-startup-frontend}
	\end{figure}
	
	Vite expose l'interface utilisateur sur \url{http://localhost:5173} avec rechargement à chaud (HMR) activé pour faciliter le développement.
	
	\section{Résultats obtenus}
	\label{sec:screenshots}
	
	Les captures d'écran suivantes illustrent les fonctionnalités principales de la plateforme GRC AI Analyzer après déploiement complet, depuis l'authentification jusqu'à la génération des résultats d'analyse.
	
	\subsection{Authentification et tableau de bord}
	
	\subsubsection{Page de connexion}
	
	L'accès à la plateforme est protégé par une authentification JWT. La page de connexion présente le branding de l'application et un formulaire de saisie des identifiants professionnels.
	
	\begin{figure}[H]
		\centering
		\includegraphics[width=\textwidth]{figures/0.png}
		\caption{Page de connexion de GRC AI Analyzer}
		\label{fig:sc-login}
	\end{figure}
	
	Après validation, le backend émet un token JWT signé (HS256) qui est stocké côté client et joint à chaque requête API via l'en-tête \texttt{Authorization: Bearer}.
	
	\subsubsection{Tableau de bord principal}
	
	Une fois authentifié, l'utilisateur est redirigé vers le tableau de bord qui agrège les indicateurs clés de l'ensemble de ses rapports~: nombre total de documents, analyses terminées, analyses en cours et risques critiques nécessitant une action immédiate.
	
	\begin{figure}[H]
		\centering
		\includegraphics[width=\textwidth]{figures/1.png}
		\caption{Tableau de bord principal avec indicateurs de conformité globale}
		\label{fig:sc-dashboard}
	\end{figure}
	
	La section \textit{Moyenne de Conformité Globale} affiche trois jauges semi-circulaires synthétisant le taux moyen de conformité aux trois référentiels supportés (ISO 27001, RGPD, Loi marocaine 09-08), calculé en agrégeant les résultats de toutes les analyses de l'utilisateur.
	
	\subsection{Gestion et analyse des rapports GRC}
	
	\subsubsection{Liste des rapports et téléchargement d'un document}
	
	La section \textit{Rapports} centralise la gestion documentaire~: zone d'upload pour les nouveaux fichiers (PDF, DOCX, XLSX) et liste des documents déjà importés avec leur statut d'analyse.
	
	\begin{figure}[H]
		\centering
		\includegraphics[width=\textwidth]{figures/2.png}
		\caption{Interface de gestion des rapports avec liste des documents}
		\label{fig:sc-rapports}
	\end{figure}
	
	Le composant d'upload valide le type MIME et les \textit{magic bytes} côté serveur avant de stocker le fichier dans le bucket MinIO. Chaque entrée affiche le format, la date d'import, le statut (\texttt{EN ATTENTE}, \texttt{EN COURS}, \texttt{TERMINE}) et propose les actions appropriées (analyser, voir l'analyse, supprimer).
	
	\begin{figure}[H]
		\centering
		\includegraphics[width=\textwidth]{figures/3.png}
		\caption{Nouveau rapport \texttt{Vul scan.pdf} importé en attente d'analyse}
		\label{fig:sc-upload}
	\end{figure}
	
	\subsubsection{Phase d'analyse asynchrone}
	
	Le déclenchement d'une analyse via le bouton \textit{Analyser} lance le pipeline asynchrone en deux phases (extraction des risques puis évaluation de conformité). Pendant le traitement, l'interface affiche un écran de chargement informatif.
	
	\begin{figure}[H]
		\centering
		\includegraphics[width=\textwidth]{figures/4.png}
		\caption{Écran d'attente pendant l'analyse IA par Mistral}
		\label{fig:sc-loading}
	\end{figure}
	
	En arrière-plan, le backend FastAPI orchestre l'extraction du texte, le chunking, l'indexation vectorielle dans ChromaDB et les appels à l'API Mistral. Les logs serveur permettent de tracer chaque étape du traitement~:
	
	\begin{figure}[H]
		\centering
		\includegraphics[width=\textwidth]{figures/backend_logs_indexing.png}
		\caption{Logs backend --- indexation ChromaDB (34 chunks) et persistance des 18 risques}
		\label{fig:sc-logs-indexing}
	\end{figure}
	
	Ces logs montrent les op\'erations critiques du pipeline~: indexation de 34~chunks dans ChromaDB, sauvegarde de 18~risques extraits, puis insertion des r\'esultats de conformit\'e dans PostgreSQL via SQLAlchemy. \textit{Note~: le score de maturit\'e affich\'e sur le \textbf{dashboard} est un agr\'egat sur l'ensemble des analyses de l'utilisateur~; le score d'une \textbf{analyse individuelle} correspond \`a la moyenne des taux de conformit\'e des trois r\'ef\'erentiels pour ce seul rapport.}
	
	\begin{figure}[H]
		\centering
		\includegraphics[width=\textwidth]{figures/backend_logs_api.png}
		\caption{Logs des requêtes API SQLAlchemy lors de la consultation d'une analyse}
		\label{fig:sc-logs-api}
	\end{figure}
	
	\subsection{Résultats de l'analyse}
	
	Une fois l'analyse terminée, l'utilisateur accède à une vue détaillée organisée en cinq onglets~: Résumé Exécutif, Cartographie des Risques, Conformités, Plan d'Action et Assistant RAG.
	
	\subsubsection{Résumé exécutif}
	
	L'onglet \textit{R\'esum\'e Ex\'ecutif} pr\'esente une synth\`ese r\'edig\'ee par Mistral incluant le contexte du document, les principales conclusions et le score de maturit\'e global. Ce score est calcul\'e comme la \textbf{moyenne arithm\'etique} des trois taux de conformit\'e~: $\overline{t} = (t_{\text{ISO}} + t_{\text{RGPD}} + t_{\text{Loi}}) / 3$. Les valeurs affich\'ees dans les captures correspondent \`a l'analyse d'un rapport de scan de vuln\'erabilit\'es sp\'ecifique~; elles varient d'un rapport \`a l'autre selon la maturit\'e de l'organisation audit\'ee.
	
	\begin{figure}[H]
		\centering
		\includegraphics[width=\textwidth]{figures/5.png}
		\caption{Résumé exécutif généré par l'IA}
		\label{fig:sc-resume}
	\end{figure}
	
	\subsubsection{Cartographie des risques}
	
	L'onglet \textit{Cartographie des Risques} affiche une matrice probabilité $\times$ impact (échelle 1--5) où chaque bulle représente un risque identifié, coloré selon son niveau de sévérité.
	
	\begin{figure}[H]
		\centering
		\includegraphics[width=\textwidth]{figures/6.png}
		\caption{Matrice des risques --- visualisation Recharts probabilité/impact}
		\label{fig:sc-risks-matrix}
	\end{figure}
	
	Sous la matrice, une liste détaillée énumère chaque risque avec sa description complète, sa catégorie, son score (P$\times$I) et son niveau de sévérité.
	
	\begin{figure}[H]
		\centering
		\includegraphics[width=\textwidth]{figures/7.png}
		\caption{Liste détaillée des risques cyber extraits par Mistral}
		\label{fig:sc-risks-list}
	\end{figure}
	
	Dans cet exemple, l'analyse d'un rapport de scan de vulnérabilités a identifié 18 risques (absence de tokens Anti-CSRF, défauts de configuration CSP, en-têtes de sécurité manquants, etc.), tous référencés par leur CWE associé.
	
	\subsubsection{Évaluation de conformité multi-référentiel}
	
	L'onglet \textit{Conformités} présente trois colonnes parallèles (ISO 27001, RGPD, Loi 09-08) avec leur taux de conformité respectif et le détail des contrôles évalués.
	
	\begin{figure}[H]
		\centering
		\includegraphics[width=\textwidth]{figures/8.png}
		\caption{Évaluation de conformité ISO 27001 / RGPD / Loi 09-08}
		\label{fig:sc-compliance}
	\end{figure}
	
	Chaque contrôle est étiqueté \textit{Conforme}, \textit{Partiel} ou \textit{Non Conforme}, accompagné d'une justification textuelle générée par l'IA expliquant l'écart constaté.
	
	\subsubsection{Plan d'action priorisé}
	
	L'onglet \textit{Plan d'Action} liste les recommandations de remédiation générées par Mistral, chacune annotée d'une priorité, d'un indicateur \textit{Quick Win} et d'un niveau d'effort estimé.
	
	\begin{figure}[H]
		\centering
		\includegraphics[width=\textwidth]{figures/9.png}
		\caption{Plan d'action priorisé avec recommandations de remédiation}
		\label{fig:sc-plan}
	\end{figure}
	
	Chaque action inclut des références normatives (OWASP, NIST SP 800-53, CWE) et un statut modifiable par l'utilisateur (\textit{À faire}, \textit{En cours}, \textit{Terminé}) pour assurer le suivi opérationnel.
	
	\subsubsection{Assistant RAG contextuel}
	
	L'onglet \textit{Assistant RAG} offre une interface conversationnelle permettant d'interroger le rapport en langage naturel. Les réponses sont contextualisées via les chunks récupérés depuis ChromaDB.
	
	\begin{figure}[H]
		\centering
		\includegraphics[width=\textwidth]{figures/10.png}
		\caption{Chat RAG contextuel --- exemple de question sur le risque le plus critique}
		\label{fig:sc-rag}
	\end{figure}
	
	L'assistant cite explicitement ses sources (\texttt{[Extrait 1]}, \texttt{[Page 4]}) garantissant la traçabilité et évitant les hallucinations~: les réponses sont strictement ancrées dans le contenu du document analysé.
	
	\subsubsection{Export PDF}
	
	La plateforme g\'en\`ere un rapport PDF complet accessible via
	\texttt{GET /api/v1/export/\{id\}/pdf}. Le document produit comprend
	une page de couverture, le r\'esum\'e ex\'ecutif, la liste des risques,
	les tableaux de conformit\'e et les recommandations prioris\'ees.
	
	\begin{figure}[H]
		\centering
		\includegraphics[width=\textwidth]{figures/pdf.png}
		\caption{Page de couverture du rapport PDF export\'e}
		\label{fig:sc-export-pdf-cover}
	\end{figure}
	
	\begin{figure}[H]
		\centering
		\includegraphics[width=\textwidth]{figures/pdf2.png}
		\caption{Page de contenu du rapport PDF --- tableau des risques}
		\label{fig:sc-export-pdf-content}
	\end{figure}
	
	\subsubsection{Export Excel (audit trail)}
	
	L'export Excel (\texttt{GET /api/v1/export/\{id\}/excel}) g\'en\`ere
	un classeur \texttt{.xlsx} multi-feuilles destin\'e \`a l'audit trail~:
	une feuille par type de r\'esultat (Risques, Conformit\'e ISO 27001,
	Conformit\'e RGPD, Conformit\'e Loi 09-08, Recommandations).
	
	\begin{figure}[H]
		\centering
		\includegraphics[width=\textwidth]{figures/excel.png}
		\caption{Export Excel --- classeur multi-feuilles pour l'audit trail}
		\label{fig:sc-export-excel}
	\end{figure}
	
	\subsection{Documentation API Swagger}
	
	FastAPI g\'en\`ere automatiquement une documentation interactive
	OpenAPI accessible \`a \texttt{http://localhost:8000/docs}.
	Cette interface Swagger UI permet de tester chaque endpoint
	directement depuis le navigateur avec authentification JWT int\'egr\'ee.
	
	\begin{figure}[H]
		\centering
		\includegraphics[width=\textwidth]{figures/docs.png}
		\caption{Documentation interactive Swagger UI (\texttt{/docs})}
		\label{fig:sc-swagger}
	\end{figure}
	
	La documentation Swagger constitue le contrat d'interface entre
	le frontend et le backend~: chaque param\`etre, sch\'ema Pydantic
	et code de retour est document\'e automatiquement \`a partir
	des annotations Python, garantissant la coh\'erence permanente
	entre le code et la documentation.
	
	\subsection{Panneau d'administration}
	
	La route \texttt{GET /api/v1/admin/users} est prot\'eg\'ee par
	la d\'ependance \texttt{require\_admin} qui v\'erifie le r\^ole
	\texttt{admin} dans le token JWT. L'interface d'administration
	permet \`a un super-utilisateur de visualiser tous les comptes
	et rapports de la plateforme.
	
	\begin{figure}[H]
		\centering
		\includegraphics[width=\textwidth]{figures/admin1.png}
		\caption{Panneau d'administration --- gestion des utilisateurs (route \texttt{require\_admin})}
		\label{fig:sc-admin1}
	\end{figure}
	
	\begin{figure}[H]
		\centering
		\includegraphics[width=\textwidth]{figures/admin2.png}
		\caption{Panneau d'administration --- gestion des r\'ef\'erentiel (route \texttt{require\_admin})}
		\label{fig:sc-admin2}
	\end{figure}
	
	\section{Bilan technique}
	
	\subsection{Métriques de performance}
	
	Les tests effectués sur des rapports réels de 10 à 80 pages donnent les résultats suivants~:
	
	\begin{table}[H]
		\centering
		\begin{tabularx}{\textwidth}{@{}>{\bfseries\color{titleblue}}l >{\raggedright\arraybackslash}p{2.6cm} X@{}}
			\toprule
			\headrow \colhead{M\'etrique} & \colhead{Valeur observ\'ee} & \colhead{Commentaire} \\
			\midrule
			Dur\'ee analyse compl\`ete & 45--120 s & D\'epend du volume et de la charge Mistral \\
			\rowcolor{rowalt} Risques extraits (moy.) & 8--18 risques & Pr\'ecision indicative \textasciitilde85\% estim\'ee par comparaison qualitative sur 5 rapports tests~; aucun protocole formel d'\'evaluation (voir limites \S\ref{sec:synthese}) \\
			Taux conformit\'e ISO & 15--75\% & Selon maturit\'e de l'organisation \\
			\rowcolor{rowalt} Latence chat RAG & $<$ 3 s & Top-5 chunks, temp\'erature 0.3 \\
			Taille collection ChromaDB & 34--400 chunks & Chunks 1500 caract\`eres, overlap 200 \\
			\bottomrule
		\end{tabularx}
		\caption{M\'etriques de performance observ\'ees}
		\label{tab:perf}
	\end{table}
	
	\subsection{Couverture fonctionnelle}
	
	\begin{table}[H]
		\centering
		\begin{tabularx}{\textwidth}{@{}X >{\centering\arraybackslash}p{2.6cm} >{\centering\arraybackslash}p{2.6cm}@{}}
			\toprule
			\headrow \colhead{Fonctionnalit\'e} & \colhead{Impl\'ement\'ee} & \colhead{Test\'ee} \\
			\midrule
			Upload PDF / DOCX / XLSX & \cyes & \cyes \\
			\rowcolor{rowalt} Extraction de risques (Mistral) & \cyes & \cyes \\
			Conformit\'e ISO 27001 & \cyes & \cyes \\
			\rowcolor{rowalt} Conformit\'e RGPD & \cyes & \cyes \\
			Conformit\'e Loi 09-08 & \cyes & \cyes \\
			\rowcolor{rowalt} Recommandations IA & \cyes & \cyes \\
			R\'esum\'e ex\'ecutif & \cyes & \cyes \\
			\rowcolor{rowalt} Chat RAG contextuel & \cyes & \cyes \\
			Export PDF & \cyes & \cyes \\
			\rowcolor{rowalt} Export Excel & \cyes & \cyes \\
			Authentification JWT & \cyes & \cyes \\
			\rowcolor{rowalt} Gestion multi-utilisateurs & \cyes & \cyes \\
			\bottomrule
		\end{tabularx}
		\caption{Couverture fonctionnelle de GRC AI Analyzer}
		\label{tab:coverage}
	\end{table}
	
	\section{Synth\`ese et limites}
	\label{sec:synthese}
	
	GRC AI Analyzer r\'epond \`a l'ensemble des besoins fonctionnels identifi\'es en chapitre~3. L'architecture asynchrone en deux phases garantit une analyse compl\`ete en moins de deux minutes pour la majorit\'e des rapports. Le pipeline RAG offre une interface conversationnelle directement ancr\'ee dans le document analys\'e, ce qui constitue un apport significatif par rapport aux solutions GRC traditionnelles.
	
	\paragraph{Limites m\'ethodologiques.}
	Trois limites m\'eritent d'\^etre explicit\'ees pour une lecture rigoureuse des r\'esultats~:
	\begin{enumerate}[leftmargin=1.5em,itemsep=4pt]
		\item \textbf{M\'etrique de pr\'ecision.} La pr\'ecision indicative de \textasciitilde85\% a \'et\'e estim\'ee par comparaison qualitative sur 5~rapports de test. Aucun protocole formel (gold standard, calcul recall/pr\'ecision, double revue ind\'ependante) n'a \'et\'e appliqu\'e. Ce chiffre est \`a consid\'erer comme un ordre de grandeur et non comme une m\'etrique valid\'ee.
		\item \textbf{Chunking fixe.} Le d\'ecoupage \`a 1\,500~caract\`eres peut couper une phrase au milieu, d\'egradant la qualit\'e de l'embedding et du retrieval. Un chunking s\'emantique (d\'ecoupage aux fronti\`eres de paragraphes) am\'eliorerait la coh\'erence des chunks.
		\item \textbf{Hallucinations LLM sur la conformit\'e.} M\^eme avec des prompts strictement cadr\'es, Mistral peut d\'eduire une non-conformit\'e ISO~27001:2022 \`a partir d'indices implicites~--- et se tromper. L'\'evaluation de conformit\'e pr\'esente dans l'interface est une aide \`a la d\'ecision, non un audit certifiant.
	\end{enumerate}
	
	Les principaux points d'am\'elioration identifi\'es pour une version de production sont~: l'ajout d'un queue distribu\'e (Celery + Redis) pour absorber les pics de charge, le passage \`a un mod\`ele Mistral fine-tun\'e sur des politiques GRC, et l'int\'egration de r\'ef\'erentiels suppl\'ementaires (SOC~2, PCI-DSS).
	
	%%===========================================================
	%% CONCLUSION GENERALE
	%%===========================================================
	\chapter*{Conclusion}
	\addcontentsline{toc}{chapter}{Conclusion g\'en\'erale}
	\markboth{Conclusion g\'en\'erale}{Conclusion g\'en\'erale}
	
	Ce projet de fin d'\'etudes a permis de concevoir, impl\'ementer et valider une plateforme compl\`ete d'analyse GRC assist\'ee par intelligence artificielle. GRC AI Analyzer automatise un processus traditionnellement long et co\^uteux~: l'analyse de conformit\'e et d'identification des risques dans les documents de gouvernance.
	
	\paragraph{Contributions techniques.}
	Sur le plan technique, le projet illustre la mise en \oe{}uvre d'un pipeline asynchrone \textit{full-stack}~: extraction documentaire multi-format (PDF/DOCX/XLSX), appels parall\`eles \`a un LLM de derni\`ere g\'en\'eration (Mistral Large), stockage vectoriel (ChromaDB) pour le RAG, et exposition via une API REST FastAPI consomm\'ee par un frontend React~18. La gestion des contraintes de production~--- limitation de d\'ebit API, validation des fichiers, authentification JWT, export multi-format~--- a fait l'objet d'une attention particuli\`ere.
	
	\paragraph{Contributions m\'ethodologiques.}
	Sur le plan m\'ethodologique, le projet d\'emontre qu'un LLM g\'en\'eraliste, correctement prompt\'e et encadr\'e par un pipeline structur\'e, peut produire des analyses GRC exploitables sans fine-tuning sp\'ecifique. Le choix de s\'erialiser les appels Mistral via un s\'emaphore \texttt{asyncio} plut\^ot que de d\'eployer une queue distribu\'ee repr\'esente un compromis pertinent pour un prototype \`a usage acad\'emique.
	
	\paragraph{Perspectives.}
	Les am\'eliorations envisag\'ees pour une version de production incluent~: l'int\'egration de Celery pour la gestion des t\^aches longues, le fine-tuning du mod\`ele sur un corpus GRC francophone, l'\'elargissement des r\'ef\'erentiels (SOC 2, PCI-DSS, NIS2), et le d\'eploiement Kubernetes avec auto-scaling des workers d'analyse.
	
	Ce travail confirme le potentiel de l'IA g\'en\'erative pour augmenter, voire automatiser partiellement, les audits de conformit\'e~--- un enjeu croissant dans le contexte r\'eglementaire marocain et europ\'een.
	
	%%===========================================================
	%% BIBLIOGRAPHIE
	%%===========================================================
	\begin{thebibliography}{99}
		\setlength{\itemsep}{4pt}
		
		%% --- Intelligence artificielle, LLM et RAG ---
		\bibitem{vaswani2017}
		A. Vaswani, N. Shazeer, N. Parmar \textit{et al.}, \enquote{Attention Is All You Need},
		\textit{Advances in Neural Information Processing Systems (NeurIPS)}, vol.~30, 2017.
		\url{https://arxiv.org/abs/1706.03762}
		
		\bibitem{lewis2020rag}
		P. Lewis, E. Perez, A. Piktus \textit{et al.}, \enquote{Retrieval-Augmented Generation for
			Knowledge-Intensive NLP Tasks}, \textit{Advances in Neural Information Processing Systems
			(NeurIPS)}, vol.~33, 2020.
		\url{https://arxiv.org/abs/2005.11401}
		
		\bibitem{mistral2024}
		Mistral AI, \textit{Mistral Large~--- Documentation et mod\`eles}, 2024.
		\url{https://docs.mistral.ai/}
		
		\bibitem{sentence_transformers}
		N. Reimers et I. Gurevych, \enquote{Sentence-BERT: Sentence Embeddings using Siamese
			BERT-Networks}, \textit{Proceedings of EMNLP-IJCNLP}, 2019.
		\url{https://arxiv.org/abs/1908.10084}
		
		\bibitem{malkov2018hnsw}
		Yu.~A. Malkov et D.~A. Yashunin, \enquote{Efficient and Robust Approximate Nearest
			Neighbor Search Using Hierarchical Navigable Small World Graphs}, \textit{IEEE Transactions
			on Pattern Analysis and Machine Intelligence}, vol.~42, n\textsuperscript{o}~4, 2020.
		\url{https://arxiv.org/abs/1603.09320}
		
		\bibitem{langchain_rag}
		LangChain, \textit{LangChain Documentation~: Building applications with LLMs through
			composability}, 2022.
		\url{https://python.langchain.com/}
		
		\bibitem{chromadb}
		Chroma, \textit{ChromaDB~: The AI-native open-source embedding database}, 2023.
		\url{https://docs.trychroma.com/}
		
		%% --- Normes, r\'eglementations et r\'ef\'erentiels GRC ---
		\bibitem{iso27001}
		ISO/IEC 27001:2022, \textit{Information security, cybersecurity and privacy protection~---
			Information security management systems~--- Requirements}, International Organization for
		Standardization, Gen\`eve, 2022.
		\url{https://www.iso.org/standard/27001}
		
		\bibitem{iso27002}
		ISO/IEC 27002:2022, \textit{Information security, cybersecurity and privacy protection~---
			Information security controls}, International Organization for Standardization, Gen\`eve, 2022.
		\url{https://www.iso.org/standard/75652.html}
		
		\bibitem{iso27005}
		ISO/IEC 27005:2022, \textit{Information security, cybersecurity and privacy protection~---
			Guidance on managing information security risks}, International Organization for
		Standardization, Gen\`eve, 2022.
		\url{https://www.iso.org/standard/80585.html}
		
		\bibitem{iso31000}
		ISO 31000:2018, \textit{Risk management~--- Guidelines}, International Organization for
		Standardization, Gen\`eve, 2018.
		\url{https://www.iso.org/standard/65694.html}
		
		\bibitem{rgpd2016}
		Parlement europ\'een et Conseil de l'Union europ\'eenne, \textit{R\`eglement (UE) 2016/679
			du 27~avril 2016 relatif \`a la protection des personnes physiques \`a l'\'egard du traitement
			des donn\'ees \`a caract\`ere personnel (RGPD)}, Journal officiel de l'Union europ\'eenne,
		L~119, 2016.
		\url{https://eur-lex.europa.eu/eli/reg/2016/679/oj}
		
		\bibitem{loi0908}
		Royaume du Maroc, \textit{Loi n\textsuperscript{o}~09-08 relative \`a la protection des
			personnes physiques \`a l'\'egard du traitement des donn\'ees \`a caract\`ere personnel},
		Bulletin officiel n\textsuperscript{o}~5714, 18~f\'evrier 2009.
		\url{https://www.cndp.ma/}
		
		\bibitem{owasp2021}
		OWASP Foundation, \textit{OWASP Top 10:2021~--- The Ten Most Critical Web Application
			Security Risks}, 2021.
		\url{https://owasp.org/Top10/}
		
		\bibitem{nistcsf}
		National Institute of Standards and Technology, \textit{The NIST Cybersecurity Framework
			(CSF) 2.0}, NIST CSWP~29, 2024.
		\url{https://doi.org/10.6028/NIST.CSWP.29}
		
		%% --- Frameworks et briques logicielles ---
		\bibitem{fastapi}
		S. Ram\'irez, \textit{FastAPI~: Modern, fast (high-performance) web framework for building
			APIs with Python}, 2018.
		\url{https://fastapi.tiangolo.com/}
		
		\bibitem{pydantic}
		S. Colvin \textit{et al.}, \textit{Pydantic~: Data validation using Python type hints}, 2023.
		\url{https://docs.pydantic.dev/}
		
		\bibitem{sqlalchemy}
		M. Bayer, \textit{SQLAlchemy~: The Python SQL Toolkit and Object Relational Mapper}, 2006.
		\url{https://www.sqlalchemy.org/}
		
		\bibitem{alembic}
		M. Bayer, \textit{Alembic~: A database migrations tool for SQLAlchemy}, 2013.
		\url{https://alembic.sqlalchemy.org/}
		
		\bibitem{postgresql}
		The PostgreSQL Global Development Group, \textit{PostgreSQL 15 Documentation}, 2022.
		\url{https://www.postgresql.org/docs/15/}
		
		\bibitem{minio}
		MinIO, \textit{MinIO High Performance Object Storage~--- Documentation}, 2024.
		\url{https://min.io/docs/minio/linux/index.html}
		
		\bibitem{react18}
		Meta, \textit{React~v18.0~--- Release Notes}, 2022.
		\url{https://react.dev/blog/2022/03/29/react-v18}
		
		\bibitem{vite}
		E. You \textit{et al.}, \textit{Vite~: Next Generation Frontend Tooling}, 2020.
		\url{https://vitejs.dev/}
		
		\bibitem{zustand}
		P. Henschel \textit{et al.}, \textit{Zustand~: A small, fast and scalable state management
			solution for React}, 2019.
		\url{https://github.com/pmndrs/zustand}
		
	\end{thebibliography}
	
\end{document}